# plays-en（英文区内容目录）

由内容生产线 `gameslog-en` 渠道（ContentPack v1.1，lang=en）物化写入。

- 内容结构同 `content/plays/`：`{slug}/article.mdx` + `meta.json`
- 当前站点前端只渲染 `content/plays/`；本目录为英文区站点/路由预留的内容源
- 禁止手工编辑：内容由生产线适配器生成，译文新鲜度由 `translationStatus` 管理
