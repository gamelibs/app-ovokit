# 2026-06-23 工作笔记

## 今日任务

清理全站面向终端用户的内部占位/提示文案。

## 完成内容

- 扫描并删除 24 个玩法 `meta.json` 中 `demo.note` 里的"建议 Demo：..."与"MVP：Demo 先留 iframe 占位。"（保留已有真实 iframeSrc 的 6 条面向用户的说明）。
- 删除玩法详情页文章区 `文章以 Markdown/MDX 文本子集渲染（不支持自定义组件）。` 说明。
- 清理 7 处 `techStack` 中的 `(占位)` 标记。
- 删除 `PlayMiniDemo.tsx` 中`建议做法：先把规则与状态机/生成器做成可复用模块，再接入真实试玩。`的开发者提示。
- 将 `NewPlayForm.tsx` 中 `demoNote` 与 `articleMdx` 默认值、AI 回填默认值置空，避免后续新内容继续出现占位文案。
- 在 `src/lib/content/plays.ts` 的 `readPlayMeta` 中增加 `demo` 字段缺失兜底（`if (!meta.demo) meta.demo = {}`），并给搜索索引使用可选链 `meta.demo?.note`。
- 在 `src/app/(site)/play/[slug]/page.tsx` 给 `play.demo?.note` 加可选链防御。

## 验证结果

- `pnpm -s typecheck`：✅ 通过
- `pnpm -s lint`：✅ 0 errors / 30 warnings
- `pnpm -s test:smoke`：✅ 68/68 页面通过

## 备注

- 版主后台顶部 `MVP：提交后写入...` 说明按用户要求保留（版主可见）。
- 文章正文中的"工程实现建议/节奏建议/曲线建议"等面向读者的专业内容未动。
