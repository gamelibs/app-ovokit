# 2026-09-20

## 支柱内容分类重构 + 内容修复批次（对齐 ovo_system taxonomy.v1.json）

用户拍板修复机检发现的 4 处站点 ↔ taxonomy 硬冲突，本次落地（ovo_system 侧 taxonomy 不动）。

### 变更摘要

1. **features 层对齐 taxonomy 8 词**：click/idle/grid/levels/numbers/merge-mechanic/roguelike/timed。
   - `content/features/merge/` → `merge-mechanic/`（key/name/nameEn 改，图目录同步 git mv；`public/icons/feature-merge.webp` → `feature-merge-mechanic.webp`）。
   - `generation`/`state-machine` 整体迁出：`content/implementation-traits/{key}/` + `public/implementation-traits/{key}/`（git mv 保历史）。
   - 新增路由 `/implementation-traits`（列表 + `[key]` 详情），镜像 /features 结构：新 lib `src/lib/implementation-traits/`（implementation-traits.ts/spec/assets/images 四文件）、新组件 `src/components/implementation-traits/`（ImplementationTraitPage/ImplementationTraitTabs，从 features 拷贝改型）。列表页标题走 i18n `implementationTraits.title`（zh 工程特征 / en Implementation Traits）。
   - 新增 timed 特征页（meta.json 用户给定全文）+ 配图：`scripts/generate-entity-assets.ts` 补 timed 映射（timing 家族场景：timing-gauge/tap/window/combo），仅 `--only=feature --key=timed` 生成 4 张 480×360 webp；首页图标 `public/icons/feature-timed.webp` 由 timing-gauge 场景渲 128×96（注意：其余 7 个图标是旧 cover-gen 风格，timed 是 sketch-svg 风格，24px 下差异不明显，将来统一重绘时可换）。
   - `src/lib/features/features.ts`：featureKeys/fallbackFeatures 重写（8 项；idle→放置产出、numbers→数值成长、merge→合成机制、新增 timed 兜底、删 generation/state-machine），新增 `featureKeyByName`（中文名→key，archetype 页 chips 用）。
2. **archetypeToPatterns.placement**：`["management"]` → `["spatial","management"]`（主 spatial 对齐 taxonomy defaultPattern，management 次级）。
3. **14 母型 features[] 收敛**为 taxonomy compatibleFeatures 中文名（match-clear=网格/关卡/数值成长/限时 等 14 项）；placement name→建造布局、nameEn→Placement / Building。
4. **concept 扩写**：features 8 个现有 + generation/state-machine（迁入 traits 后概念照换）整体替换 ≥120 字；patterns 5 个（management/merge/narrative/spatial/strategy）末尾追加句子后均 ≥130 字（action 原文已 132 未动）。
5. **重定向与 sitemap**：`next.config.ts` redirects 显式 `statusCode: 301`（Next permanent:true 默认 308，用户要求 301 字面）——merge→merge-mechanic、generation/state-machine→implementation-traits/{key}，zh + /en 两组。sitemap features 段自动跟随 featureKeys，新增 implementation-traits 列表 + 2 详情双语条目。
6. **版主编辑**：/mod/features 跟随新 keys（无需改代码）；新增 /mod/implementation-traits 列表 + `[key]/edit` + `/api/mod/implementation-traits`（GET 列表 / POST meta / images POST+DELETE），鉴权复用同一套。`FeatureEditForm`/`FeatureImageUpload` 增加 `apiBase`/`publicBase`/`backHref`/`submitLabel` 可选参数复用（EditableFeatureSpec = Omit<FeatureSpec,"key"> & {key:string}）。/mod 首页加「工程特征管理」入口。
7. **名称映射对齐**：`ArchetypePage` 玩法特征区从纯文本 join 改为 chips（Link 到 /features/{key}，经 featureKeyByName 解析）；`plays.ts` legacyCatKeyMap.merge→cat "merge-mechanic"；features 列表页默认选中 merge→click。

### 验证证据

- `pnpm typecheck` 0 错；`pnpm build` 成功（新路由全部出现在产物路由表）。
- PM2 进程 ovo-site-becf127c 当时不存在，走 ovo_system 受管通道 `POST /api/site/becf127c-e6b3-44c1-9ea2-2939ad5583a1/launch`（等价重启，端口 19600）。
- Playwright 36 项断言全 PASS（console 零 error）：/features 8 项无 generation/state-machine；/features/timed 200 + 4 图 200；4 条 301（zh+en）Location 正确；/implementation-traits 列表/详情 200；placement 建造布局 + chips 网格/关卡/数值成长且链接可达；match-clear chips 含限时；patterns 6 页 concept 132~155 字。
- 首次跑出现 1 次 React #418（hydration）——复核为连续快速导航的瞬态，单页逐一复访 9 页全部 clean，重跑全套 36 项 PASS。
- 截图 /tmp/pillar-restructure-*.png（7 张：features-list/timed/merge-mechanic/trait-generation/placement/match-clear/pattern-management）。
- sitemap.xml 含 implementation-traits 3 条目（含 hreflang）；/mod/implementation-traits 200（未登录渲染提示页），API 未登录 401（与 features 一致）。

### 遗留 / 注意

- **未提交 git**（用户明确要求不做提交）。
- `feature-generation.webp`/`feature-state-machine.webp` 图标留在 public/icons/（已无任何引用，将来可清理）。
- traits 详情页未挂 FavoriteButton（FavoriteType 是封闭 union，避免为工程层扩收藏类型；如需收藏要动 favorites types/card/messages）。
- 首次跑全量 redirects 时 next-redirects 曾用 permanent:true（308），已改为显式 301 并重新 build+launch。
