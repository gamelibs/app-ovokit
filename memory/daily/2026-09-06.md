# 2026-09-06 工作笔记

> 会话目标：禁用运行时图片优化 + 剔除 sharp 原生模块 + 重新发布并推送（产物平台无关化）。

---

## 已完成

### 1. 禁用运行时图片优化
- `next.config.ts` 加 `images: { unoptimized: true }`（headers() 逻辑不动）。
- 动因：站点图片全部是内容生产期预生成的 webp，运行时 `/_next/image` 优化是纯冗余；禁用后 next/image 直接输出原始 URL，且构建不再依赖 sharp。

### 2. release.sh 剔除 sharp 死代码
- 新增「剔除平台相关原生模块（sharp）」步骤（组装产物后、提交前）：删除 `node_modules/sharp`、`node_modules/@img`、`.pnpm/` 下 `@img+sharp*` 与 `sharp@*` 条目；清理悬空 symlink；兜底断言无 sharp 残留与 `.node` 原生二进制（有则 exit 1）；打印剔除前后体积对比。
- macOS bash 3.2 兼容（`${VAR}` 花括号），脚本头注释同步更新。

### 3. 发布与验证
- main 提交 `6225164`（next.config + release.sh）。
- `bash scripts/release.sh`：构建 BUILD_ID `FyHKiMxklp7ebnqdNYaqy`；**node_modules 72M → 38M**，产物总体积 104M → **70M**；deploy 分支快进提交 `efe1327`（`产物：main 6225164（2026-09-06T03:06:08Z）`）。
- 产物独立验证（git archive 到 /tmp，测试 .env.local，裸 `PORT=13199 node server.js`，非 PM2）：
  - node_modules 无 sharp/@img、无 `.node` 文件、无悬空 symlink
  - 首页 200、内容页 `/play/2048-variant-probability-control` 200、封面直出 200
  - **HTML 中 `/_next/image` 出现 0 次**，图片为 `src="/plays/.../cover.webp"` 直链（unoptimized 生效的直接证据）
  - run.log 无 sharp/error 记录；验证后进程已杀、临时目录已删

### 4. 推送（SSH 443 端点）
- 本机到 github.com 的 SSH 22 传大包会断，统一走 443：`GIT_SSH_COMMAND='ssh -o Hostname=ssh.github.com -o Port=443'`。
- `main`：e5eda21..6225164；`deploy/gameslog.top`：2b7d9a2..efe1327（含早前 6c53f5e/2b7d9a2 两个本地产物提交一并推上）。
- `git ls-remote origin` 与本地 `git rev-parse` 两端哈希一致。

## 后续

- 服务器侧：`git clone -b deploy/gameslog.top …`（或已有仓库 `bash deploy.sh update`）→ 配 `.env.local` → `bash deploy.sh start`；产物已与服务器平台无关（无 darwin 二进制），Ubuntu 直接可跑。
- 若未来重新启用运行时图片优化，需同时回滚 next.config.ts 的 unoptimized 与 release.sh 的 sharp 剔除步骤。


---

## 第二轮：ovoforge → gameslog 命名空间全量迁移 + deploy.sh 防呆加固 + 重新发布

> 会话目标：消除服务器 PM2 命名冲突（残留 ovoforge-web 旧进程被误 reload），统一 gameslog 命名空间并重新发布推送。

### 1. 背景（服务器事故驱动）
- Ubuntu `/opt/app-ovokit` 上 PM2 残留旧部署的 `ovoforge-web` 进程（旧站点已改作 pixel-studio）。
- 产物版 deploy.sh 发现同名进程直接 `pm2 reload` → 实际重启的是旧代码，新产物未启动；健康检查的 200 是旧进程响应的。
- 另有 `.env.local` 属主问题（root 600，ubuntu 读不到），服务器侧由用户在主会话处理。

### 2. 改名（main commit `ba2d422` + `baf2093`）
- PM2：ovoforge-web → **gameslog-web**（deploy/ecosystem.config.js、ecosystem.web.config.js、deploy.sh）；ovoforge-algo-api → **gameslog-algo-api**（ecosystem.algo.config.js、README.md）
- package.json：app-ovoforge → **app-gameslog**；docker-compose 服务/容器/镜像/网络名全改 gameslog
- localStorage：`gameslog:liked:` / `gameslog:viewed:` / `gameslog:edit-play:` / `gameslog:new-play:draft:v1` / `gameslog:analyzer-draft` / `gameslog_favorites` / `gameslog-cookie-consent` / `gameslog_mod_tools`
- 自定义事件 `gameslog:favorites:change`；CSS 变量 `--gameslog-scroll-x`
- cookie `gameslog_mod`（HMAC signer 同步改 `gameslog-mod-signer`，旧签名 cookie 失效需重新登录）
- Redis 前缀 `gameslog:stats`；隐私页文案同步
- 保留：TopNav 的 `ovoforge_mod_tools` legacy 迁移块；`platform-import.ts` 注释（ovo_system 侧真实 profile 名 ovoforge-v1）
- 本次指令覆盖 2026-09-02「技术命名空间保持不变」的旧决策

### 3. deploy.sh 防呆加固（main commit `baf2093`）
- `pm2_app_cwd()`：pm2 jlist + node 解析同名进程 pm_cwd；本地 5 用例验证（命中/不匹配/非法 JSON/空数组/缺字段，异常全部输出空串不崩）
- `guard_same_name_app()`：cmd_start/cmd_restart 在 reload 前校验同名进程 cwd == 当前目录，不一致则拒绝执行并提示 `pm2 describe` / `pm2 delete` 流程；取不到 cwd 只 warn 不误杀
- `port_in_use()`（ss/lsof 双分支）：pm2 start 前期望端口已被占用则拒绝启动并给排查命令
- `bash -n` 语法检查通过

### 4. 发布与验证
- `pnpm typecheck` 通过（包名已显示 app-gameslog）
- `bash scripts/release.sh`：30s 构建，产物提交 `ac8686f`（`产物：main baf2093（2026-09-06T04:43:18Z）`）
- 产物独立验证（git archive → /tmp，测试 .env.local，裸 `PORT=13199 node server.js`，非 PM2）：
  - 三件套命名确认 gameslog-web，无 ovoforge 残留
  - 首页 200、内容页 `/play/2048-variant-probability-control` 200、封面 200
  - 首页 HTML 零 ovoforge；run.log 无报错；验证后进程已杀、临时目录已删

### 5. 推送（SSH 443 端点）
- `main`：5828c78..baf2093；`deploy/gameslog.top`：404ac7c..ac8686f
- `git ls-remote origin` 与本地 `git rev-parse` 两端哈希一致（main=baf2093，deploy=ac8686f）

### 6. 服务器侧待办（转述给用户，在主会话处理）
1. `pm2 describe ovoforge-web` 确认旧进程归属（预期是旧 pixel-studio 部署残留）
2. 确认是残留后 `pm2 delete ovoforge-web`
3. 修正 `.env.local` 属主：`sudo chown ubuntu:ubuntu .env.local`（保持 600）
4. `cd /opt/app-ovokit && bash deploy.sh update && bash deploy.sh start`（新脚本拉起 gameslog-web）
5. 顺带检查 pixel-studio 用什么进程名/端口运行，避免与 gameslog-web（13100）冲突


---

## 第三轮：站点本地预览始终跟随最新版本（跨仓库）

> 会话目标：消除「release 后本地 :19600 实例不自动重启、预览看到旧版本」的缺口（已连续两次造成困惑：ChunkLoadError、关于页旧内容）。

### 1. Part 1：release.sh 发布后自动重启本地实例（main commit `eb8459b`）
- 新增第 5 步「本地预览自动重启」：产物发布完成后，从 `ovo-site.json` 解析 siteId（node 读取 JSON），网关 `localhost:19527` 可达时调用 `POST /api/site/{siteId}/launch-stop` + `launch` 重启 ovo-site 托管实例，再探测 `localhost:19600/` 200（最多 30s）并打印「本地预览已更新到最新构建」。
- 全程短超时 + 非致命（网关不可达/重启失败只警告不中止发布）；`RELEASE_NO_LOCAL_RESTART=1` 可跳过；脚本头注释同步说明。

### 2. Part 2：ovo_system 站点管理加版本状态（ovo_system 不提交 git，热重载生效）
- 后端 `GET /api/site/:platformId/version-status`（site-launcher.ts `versionStatus()`）：
  - `running/startedAt`：复用 pm2 jlist 的 pm_uptime（进程启动时刻）
  - `build`：repoPath 下 `.next/BUILD_ID` 内容 + 文件 mtime（不存在为 null）
  - `release`：`git branch -r --list 'origin/deploy/*'` 探测 deploy 分支（取第一个，本地分支兜底），`git log -1 --format=%h|%cs|%s`（execFile，5s 超时，失败 null）
  - `stale`：running 且 startedAt < build.mtime（构建比进程新 = 运行旧版）
  - repoPath 缺失/非 git 仓库时字段全 null，不 500
- 前端站点管理页 external 卡片新增版本行（`运行中（MM-DD HH:MM 启动） · 构建 XXXX（MM-DD HH:MM） · 最新发布 XXXX（MM-DD）`）；stale 时显示橙色「有更新未重启」徽标 +「重启加载最新」按钮（launch-stop + launch，完成后 refetch）

### 3. 验证（全部真实执行）
- ovo_system 双端 typecheck 0 报错（runtime-gateway tsc / admin-web vue-tsc）
- version-status 三次 curl：正常 stale=false → touch BUILD_ID 后 stale=true → launch-stop+launch 重启后 stale=false（startedAt 越过 build mtime）
- 真实 `bash scripts/release.sh`（28s）：产物提交 `0626952`；自动重启段输出「✓ 本地预览已更新到最新构建（http://localhost:19600）」；`curl localhost:19600/about` 200；version-status 恢复 stale=false
- Playwright 截图（ovo_system/tmp/）：`site-version-stale.png`（橙色徽标 + 重启按钮）、`site-version-normal.png`（正常态）

### 4. 推送（SSH 443）
- main：00f3016..eb8459b；deploy/gameslog.top：692be28..0626952（release 产生的新产物提交）
- 两端哈希一致（main=eb8459b，deploy=0626952）

### 5. 行为说明与遗留
- `release` 字段显示的是「远端已发布的最新产物提交」：本地 release 后未 push 时仍显示旧值，push 后自动跟上（语义正确，不是 bug）
- ovo_system 侧改动（site-launcher.ts / site-controller.ts / site.ts / siteApi.ts / SiteManagementPage.vue）按用户要求未提交 git，由热重载生效
- 模板站点（generated）的构建链路另有一套，本次只覆盖 external local 站点

---

## 第四轮：站点内容生产线设计定稿与存档（跨仓库）

> 会话目标：明确内容生产模型与管线归属，输出详细设计文档并双份存档。

### 1. 内容模型纠正与确认
- 用户纠正：不是按样本逐个写稿，而是**双层模型**——12 行为母型为框架层（pillar 常青页），个体游戏文章 + 试玩 Demo 为案例层（cluster 实例流，回链母型页输权重）。
- 行业验证：pillar+cluster（HubSpot SEO 模型）/ pattern language（《A Pattern Language》）；游戏业同类 Game Programming Patterns、ncase.me、Game Maker's Toolkit、Deconstructor of Fun；其它行业 Laws of UX、Mobbin、Refactoring.Guru、Hooktheory Theorytab、Salt Fat Acid Heat。

### 2. 归属与插件化决策
- 生产线建在 **ovo_system（生产端）**，站点永远只渲染；两侧唯一契约 = ContentPack。
- 用户硬要求：功能模块插件化组合、不为 gameslog 写死、可为所有需要内容输出的站点服务 → 四层插件（选题源/生成器/质量门禁/渠道适配器）+ 渠道 profile JSON，gameslog 仅为首个渠道；反硬编码规则写入设计文档 §8。

### 3. 文档落盘
- 权威版：ovo_system `docs/site-content-production-line.md`（ContentPack schema v1、适配器接口、管线状态机、M0→M3 实施路线、待核实清单）。
- 本站存档：`doc/站点内容生产线说明.md`；`doc/AGENT_CONTEXT.md` 已加索引；BACKLOG 新增 task:content-pipeline-001（P1，待立项）。

### 4. 目录改名评估（app-ovoforge-site → gameslog-site，待确认执行）
- 必改 2 处：`ovo_workspace/control/sites/becf127c-*.json` 的 `repoPath`；PM2 本地实例 `ovo-site-becf127c` 的 cwd（须 launch-stop → mv → 改注册表 → launch 重建）。
- 不受影响：git 远程/分支、服务器部署（/opt/app-ovokit）、release.sh。
- cosmetic 残留：ovo_system 文档约 19 处旧名提及；本仓库仅 `public/demos/atomic/core.js` 一处字符串。

---

## 第五轮：内容生产线实施落地 + 目录改名执行（跨仓库）

> 会话目标：执行目录改名 + 实施整条内容生产线。

### 1. 目录改名（已执行）
- launch-stop → `mv app-ovoforge-site gameslog-site` → 注册表 repoPath 改 `../../ovo/gameslog-site` → launch 重建：PM2 `ovo-site-becf127c` cwd 已在新路径，`localhost:19600` 200、version-status 正常（stale=false）、`pm2 save` 已持久化。

### 2. 后端（ovo_system，未提交 git）
- `apps/runtime-gateway/src/services/content-pipeline/`：types/db/registry/engine + 选题源×2（taxonomy-coverage、game-library）+ 生成器×2（manual、forge-studio）+ 门禁×3（dedup、seo-fields、structure）+ gameslog-mdx 适配器；`/api/content-pipeline/*` 17 条路由（含后补的 publish-records）；SQLite `ovo_workspace/runtime/content-pipeline/content-pipeline.db`（7 表）。
- 验收：typecheck 0；curl 全流程（选题 14 条→草稿→manual 成稿→门禁→发布落盘 meta.json+article.mdx→pnpm build 成功→unpublish 清理回 31 slug）；门禁 fail 409 阻断；forge-studio 离线 503。

### 3. 前端（ovo_system admin-web，未提交 git）
- 新页 `/content-pipeline`（选题队列/草稿审核台/渠道管理/发布记录）+ `contentPipelineApi.ts` + 路由与顶部导航注册；`vue-tsc` 0 报错；Playwright 冒烟截图 `ovo_system/tmp/content-pipeline-*.png`（选题 14 行、门禁红绿报告、发布→下线全链路 UI 演示）。

### 4. 事实修正与缺口
- taxonomy 实为 **14 母型**（merge-unit、turn-duel 超出本站现有 12，缺 pillar 页）。
- 分析库仅 1 条 classification；4000+ 游戏库真实路径未确认（`ovo_workspace/control/games/` 为 5 个平台目录）→ game-library 选题源已做成可配置路径，待接入。
- 遗留：unpublish 后覆盖度口径、forge-studio input 契约、M3 多站点适配器。
