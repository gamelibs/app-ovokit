# 2026-06-21 工作笔记

> 会话目标：评估网站上线 readiness，更新 Agent 记忆，启动并完成基础一期（P0）上线交付准备。

---

## 已完成

### 1. 上线前综合评估
- 读取 `memory/` 全部状态文件与 `doc/` 核心文档。
- 运行健康检查：`pnpm build` 通过；`pnpm typecheck` 需在 clean build 后通过；`pnpm lint` 122 errors / 1754 warnings。
- smoke test：首页、详情页、母型、原型、特征、合规页均返回 200。
- 从正确性、稳定性、内容、交互、安全、合规、性能、运维等维度输出详细评估结论。

### 2. Agent 记忆与工作流升级
- 重写 `memory/STATUS.md`：阶段改为“基础一期（P0）上线交付准备阶段”，更新阻塞项与健康检查。
- 重写 `memory/BACKLOG.md`：新增 P0 阻塞项、内容补齐、工程优化任务，并添加机器可读的 `task:id` 元数据。
- 新建 `memory/daily/2026-06-21.md`。
- 实现 `scripts/agent-task-runner.ts`：半自主任务推进脚本，支持 `pnpm agent:next` / `pnpm agent:dry`。
- 新增 `doc/agent-task-runner.md` 文档。
- 在 `package.json` 中添加 `agent:next`、`agent:dry`、`agent:run`、`generate:entity-assets` 脚本。

### 3. 安全与代码质量（P0 阻塞项）
- **版主鉴权加固**：`src/lib/mod/auth.ts` 改为 HMAC-SHA256 签名 cookie；`login`/`logout` 路由同步更新。
- **修复 block-editor bug**：`src/features/block-editor/useBlockEditor.ts` 将 `commit` 提前到 useEffect 前，并使用 `useCallback`。
- **治理 lint errors**：通过子代理批量修复 10+ 个文件的 `setState in effect`、`explicit any`、`require()` 等问题；`eslint.config.mjs` 忽略第三方 `games/ReferenceCase/**` 和 `public/**`。
- 新增 `src/lib/hooks/useLocalStorage.ts` 辅助 Hook，避免在 effect 中直接读写 localStorage 并 setState。

### 4. 稳定性与持久化
- **统计迁移到 Redis**：`src/lib/content/views.ts` 改用 Upstash Redis（`HINCRBY`），保留文件系统 fallback；新增 `src/lib/redis.ts`。
- **API 频率限制**：新增 `src/lib/rate-limit.ts`，对 view/like/contact/login 应用基于 IP 的限流。
- `.env.example` 增加 `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN`。
- 新增 `scripts/migrate-stats-to-redis.ts` 迁移脚本。

### 5. 内容与图片补齐
- **母型↔核心原型映射**：`src/lib/archetypes/archetypes.ts` 新增 `archetypeToPatterns`；`pageModel.ts` 暴露 `patternKeys`；`ArchetypePage.tsx` 展示所属核心原型链接。
- **程序化 SVG 生成**：新建 `scripts/generate-entity-assets.ts`，为 12 母型 + 5 原型 + 9 特征生成 104 张说明图（hero/interaction/rule/advanced）。

### 6. 体验优化
- **移除外部 CDN 字体**：`src/app/layout.tsx` 移除 jsDelivr `lxgw-wenkai-webfont` 链接；`src/app/globals.css` 移除未使用的 `--font-wenkai` 变量。

### 7. 验证
- `pnpm typecheck` ✅ 通过
- `pnpm build` ✅ 通过
- `pnpm lint` ✅ 0 errors / 33 warnings
- smoke test：核心页面均 200

## 关键发现

- 初始版主鉴权仅靠 cookie 值 `"1"`，存在伪造风险。
- `data/plays-views.json` 文件系统统计不适合生产 serverless 环境。
- 工作区存在大量未提交修改与未跟踪文件，需整理。

## 下一步

1. 整理 Git 工作区并分批提交上线版本。
2. 运行最终上线前验证。
3. 配置生产环境变量并部署。

---

## 新增：服务器部署准备（同日晚间）

### 已完成

1. **撰写生产服务器部署清单** `doc/server-deployment-checklist.md`
   - 覆盖 CentOS/RHEL/Rocky + PM2 + Nginx + SSL + Upstash Redis。
   - 明确说明不推荐维护独立 `server` 分支，推荐 `main` 单源 + GitHub Actions 自动部署。
   - 包含环境初始化、防火墙、安全加固、Nginx 配置、回滚、FAQ。

2. **实现部署环境检查脚本** `scripts/deploy-check.sh`
   - 检查：用户、OS、Node/pnpm/git/pm2/curl/nginx、Git 状态、依赖完整性、PM2 配置、环境变量、`.env.local` 权限、MOD_PASSWORD 强度、Redis 连通性、构建产物、端口监听、Nginx/SSL、防火墙、健康检查。
   - 输出彩色日志，最终给出 ✅ 通过 / ⚠ 警告 / ❌ 失败结论，退出码 0/1 表示是否符合部署条件。
   - 已适配 macOS bash 3.2 与 Linux bash 4+/5+。

3. **补齐 PM2 生产配置**
   - 新增 `ecosystem.web.config.js`（Next.js web，端口 13100）。
   - 更新 `ecosystem.algo.config.js` 使用 `algo:start`（生产模式）。
   - `package.json` 新增 `web:pm2`、`algo:start`、`deploy:check`、`engines`（node >=20, pnpm >=9）。

4. **创建 GitHub Actions 自动部署工作流** `.github/workflows/deploy.yml`
   - 触发条件：`push` 到 `main` 或手动 `workflow_dispatch`。
   - 通过 SSH 到服务器：拉取 → 安装 → 运行检查 → 构建 → PM2 reload web + algo。
   - 需要仓库 Secrets：`DEPLOY_HOST`、`DEPLOY_USER`、`DEPLOY_SSH_KEY`、`DEPLOY_PATH`，可选 `DEPLOY_PORT`。

### 验证

- `bash scripts/deploy-check.sh` 在开发环境正常输出，正确识别 macOS 下缺失 pm2/nginx/Redis 等问题。
- `pnpm -s typecheck` ✅ 通过
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅ 通过

### 关键决策

- **不再维护 `server` 分支**：以 `main` 为唯一上线源，CI/CD 自动部署，避免分支漂移与合并冲突。
- **服务器保留完整仓库**：在服务器本地构建，便于回滚与调试；未来如需更轻量，可改为构建产物 rsync。
- **`.env.local` 不进入 GitHub Secrets/CI**：只在服务器上管理，CI 只负责代码与重启，降低密钥泄露面。

---

## 新增：项目无用资源清理（同日晚间）

### 已完成

1. **扫描资源引用**
   - 确认 `imgs/`（2 MB WebP 概念图）没有任何代码或页面引用。
   - 确认 `public/file.svg`、`public/globe.svg`、`public/next.svg`、`public/vercel.svg`、`public/window.svg` 为 Next.js 默认资源，未被引用。
   - `games/ReferenceCase/sliding-puzzle-3d` 仍被 `/embed/games/...` 与 `sliding-puzzle-3d-photo` 帖子引用，保留。
   - `public/plays/_placeholders/` 仍作为代码兜底引用，保留。

2. **删除无用资源**
   - 删除 `imgs/` 整个目录（约 2 MB）。
   - 删除未使用的默认 public SVG 5 个文件。
   - 删除项目中的 `.DS_Store` 文件（`./.DS_Store`、`comfyui/.DS_Store`）。

3. **更新脚本与文档**
   - `scripts/compress-images.ts`：移除 `imgs/` 默认值，改为必须显式指定 `--input-dir` 和 `--output-dir`；新增 `--help` 与使用说明。
   - `scripts/generate-play-covers.ts`：默认模式改为 `programmatic`；AI 模式必须显式指定 `--input-dir`；新增 `--help`。
   - `package.json`：`compress:images` 与 `generate:covers:ai` 改为输出帮助信息。
   - `doc/图片压缩脚本说明.md` / `doc/玩法封面生成脚本说明.md`：移除 `imgs/` 相关说明，更新为通用用法。

### 验证

- `pnpm -s typecheck` ✅
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅

### 后续补充清理

- `comfyui/workflows/minigame_concepts_3x4_and_4x3.json` 原仅被 README 与文档引用，已确认删除。
- 已同步更新 `README.md`（移除 ComfyUI 工作流章节）、`AGENTS.md`、`doc/AGENT_CONTEXT.md` 的默认忽略目录列表。

---

## 新增：修复 hydration mismatch 与全站 smoke test（同日晚间）

### 问题

用户反馈 `/archetypes` 页面出现 React hydration 错误：

```
Hydration failed because the server rendered HTML didn't match the client.
```

根因：多个 Client Component 在渲染阶段直接使用 `typeof document !== "undefined"` / `typeof window !== "undefined"` 分支，导致服务端与客户端首次渲染结果不一致。

### 已修复的组件

| 文件 | 问题 | 修复方式 |
|---|---|---|
| `src/components/demos/DemoEmbed.tsx` | 全屏按钮基于 `document.fullscreenEnabled` 条件渲染 | 使用 `useCanFullscreen()` |
| `src/components/demos/FullscreenStage.tsx` | 同上 | 使用 `useCanFullscreen()` |
| `src/components/demos/GameShell.tsx` | 同上，且 hook 被条件调用 | 使用 `useCanFullscreen()`，新增 `showFullscreenButton` |
| `src/components/site/TopNav.tsx` | 搜索框初始值读取 `window.location.search` | 改为非受控输入，`defaultValue` 通过 `useClientValue` 获取 |
| `src/components/cookie/CookieConsent.tsx` | 横幅可见性在 effect 中 setState | 使用 `useClientValue` + `dismissed` 状态 |
| `src/components/analytics/GoogleAnalytics.tsx` | GA 加载状态在 effect 中 setState | 使用 `useClientValue` |

### 新增 Hook

- `src/lib/hooks/useClientValue.ts`：基于 `useSyncExternalStore`，hydration 期间使用服务端快照，完成后切换到客户端值。
- `src/lib/hooks/useCanFullscreen.ts`：基于 `useClientValue` 的全屏能力检测。

### 新增全站 smoke test

- 脚本：`scripts/smoke-test.ts`
- 依赖：`puppeteer-core`（使用系统 Chrome，无需下载浏览器）
- 命令：`pnpm test:smoke`
- 覆盖：68 个页面（首页、合规页、母型/原型/特征列表与详情、30 个玩法详情、版主后台、Demo 页）
- 检查项：
  - HTTP 200
  - 无未捕获异常
  - console.error 中无 hydration 错误
  - 页面存在 `<main>` 或 `<h1>`
  - 无 React Runtime / Hydration 错误遮罩

### 验证结果

- `pnpm -s typecheck` ✅
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm test:smoke` ✅ 68/68 通过

### 提交

```
[fix] 修复 hydration mismatch 并新增全站 smoke test
```


---

## 新增：`doc/` 文档整理（同日最终）

### 已完成

1. **审阅 `doc/` 全部 17 个 `.md` 文件**
   - 识别状态：必读保留 / 需刷新 / 过时可删除 / 可合并到 `memory/`。

2. **归档过时文档**
   - 将 `doc/站点风格说明.md`、`doc/内容体系诊断与补齐方案.md`、`doc/项目进度说明.md` 移入 `doc/archive/`：内容已被新文档或 `memory/` 覆盖。

3. **刷新关键文档**
   - `doc/OVOKIT_V1_生产级实施方案.md` → 改为 **V1 上线总结**，更新为：30 篇玩法、12 母型、5 原型、9 特征、68 页 smoke test 通过、P1/P2 规划起点。
   - `doc/OVOKIT_V2_战略白皮书_2026.md` → 刷新“当前状态评估”基线，内容模型改为当前四层体系（Play / Archetype / Prototype / Feature）。
   - `doc/开发说明.md` → 补充原型/特征路由与数据层、母型 Demo 已可玩、更新构建/lint/smoke 验证状态。
   - `doc/手绘风格契约.md` → “待决策项”改为“已决策项”：废弃 Dark Mode、卡片伪随机稳定、全部使用 RoughJS。
   - `doc/版主工作流.md` → 鉴权描述改为 HMAC 签名 Cookie。
   - `doc/AGENT_CONTEXT.md` → 补充 `server-deployment-checklist.md`、核心原型/特征入口索引、`pnpm test:smoke`。
   - `doc/AGENT_WORKFLOW.md` → 刷新“当前阶段重点”为生产部署 + P1 体验优化 + P2 能力扩展。

4. **新增文档索引**
   - 新建 `doc/README.md`，按必读、开发、内容、脚本、规划等分类索引全部 `doc/` 文档。

5. **同步记忆文件**
   - 更新 `memory/STATUS.md`：标记文档整理完成，下一步为生产服务器实际部署。
   - 更新 `memory/BACKLOG.md`：将文档整理移入 Done，新增 P0 任务“生产服务器实际部署”。

### 验证

- 所有修改仅限 `doc/` 与 `memory/`，未改动代码。
- `pnpm -s typecheck` ✅ 通过
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅ 通过

### 关键决策

- 不再维护独立的 `server` 分支：以 `main` 为唯一源 truth，通过 GitHub Actions 自动部署。
- `doc/` 目录以“上线事实 + 规划起点”为主线，删除重复/过时内容，降低后续 Agent 阅读噪音。


---

## 新增：Docker 容器化部署方案（同日追加）

### 背景

用户反馈服务器系统环境过旧，希望改用 Docker 部署以隔离运行环境。

### 已完成

1. **新增 `Dockerfile`**
   - 基于官方 `node:20-slim` 镜像，安装 `python3/make/g++/git/curl` 等必要依赖。
   - 多阶段构建：`deps` → `builder` → `runner`。
   - `NEXT_PUBLIC_*` 通过 `build args` 注入，客户端 bundle 正确。
   - `.env.local` 不在镜像中复制，运行时通过 `env_file` 挂载，避免泄露密钥。

2. **新增 `docker-compose.yml`**
   - `ovoforge-web`：运行 `pnpm start`，暴露 `127.0.0.1:13100`。
   - `ovoforge-algo`：运行 `pnpm algo:start`，暴露 `127.0.0.1:14100`。
   - 两个服务只绑定回环地址，外部通过宿主 Nginx 访问。
   - 添加 healthcheck、restart policy、自定义网络 `ovoforge`。

3. **新增 `.dockerignore`**
   - 排除 `node_modules`、`.next`、`.git`、`.env*`、日志、编辑器配置、归档文档等。

4. **新增 `doc/docker-deployment.md`**
   - 完整 Docker 部署指南：安装 Docker、克隆代码、`.env.local`、构建启动、Nginx 配置、SSL、日常更新、常见问题。
   - 说明与 GitHub Actions 自动部署结合的方式。

5. **同步文档索引与记忆文件**
   - `doc/README.md`、`doc/AGENT_CONTEXT.md` 增加 Docker 部署文档入口。
   - `memory/STATUS.md`、`memory/BACKLOG.md` 更新为「Docker 部署待执行」。

### 关键设计决策

- **前后端共用一份镜像**：降低维护成本；algo 容器通过 `command` 覆盖启动。
- **应用端口仅绑定 `127.0.0.1`**：继续由宿主 Nginx 处理 SSL、域名、静态缓存，复用用户已有 nginx 配置。
- **密钥不进入镜像**：`.env.local` 运行时挂载；公开变量通过 build args 注入。
- **算法后端可选**：站点内 Demo 实际由 Next.js `/api/demos/*` 处理，algo 服务保留给未来扩展。

### 验证

- `pnpm -s typecheck` ✅ 通过
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅ 通过

> 注：Docker 镜像本身未在本机完整构建验证，建议在服务器上执行 `docker compose up --build -d` 后按 `doc/docker-deployment.md` 验证。


---

## 新增：修复 Docker 镜像运行时 500（同日追加）

### 问题

用户部署 Docker 后，`ovoforge-web` 容器健康检查失败，`curl http://127.0.0.1:13100/` 返回 500。日志显示：

```
Error: ENOENT: no such file or directory, scandir '/app/content/plays'
```

根因：`Dockerfile` 的运行层（runner）只复制了 `.next`、`public`、`server`、`package.json`、`node_modules`，漏掉了运行时读取的 `content/` 与 `games/` 目录。

### 修复

1. **更新 `Dockerfile`**：在 runner 阶段增加
   ```dockerfile
   COPY --from=builder /app/content ./content
   COPY --from=builder /app/games ./games
   ```

2. **更新 `docker-compose.yml`**：增加 `volumes` 挂载
   - `./data:/app/data`：持久化浏览/点赞统计（Redis 未配置时的 fallback）
   - `./content/contact-messages:/app/content/contact-messages`：持久化联系表单留言

3. **更新 `doc/docker-deployment.md`**：补充说明运行时目录复制与卷挂载策略，并将示例路径统一为 `/www/ovoforge`。

### 验证

- `pnpm -s typecheck` ✅ 通过
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅ 通过

> 注：由于本地无 Docker 环境，未实际运行容器验证；建议用户在服务器执行 `docker compose down && docker compose up --build -d` 后验证。


---

## 部署上线完成（同日最终）

### 结果

OVOFORGE 已成功通过 Docker 部署到生产服务器并正式上线：

```bash
$ curl -I https://ovoforge.com/
HTTP/2 200
server: nginx/1.20.1
x-powered-by: Next.js

$ curl -I http://127.0.0.1:13100/
HTTP/1.1 200 OK
X-Powered-By: Next.js
```

### 最终架构

- 容器：
  - `ovoforge-web`：Next.js 前端，`127.0.0.1:13100`
  - `ovoforge-algo`：Fastify Demo 后端，`127.0.0.1:14100`
- 反向代理：宿主 Nginx（`ovoforge.com.conf`）→ `http://127.0.0.1:13100`
- SSL：certbot + Let's Encrypt
- DNS：Cloudflare A 记录指向服务器 IP，当前为 DNS only（灰色云朵）

### 已解决的关键问题

1. Docker 镜像缺失 `content/` / `games/` → 已补复制。
2. `data/` 与 `content/contact-messages/` 持久化 → 已加 volumes。
3. 品牌/服务名统一 `ovoforge` → 已完成全局替换。
4. GitHub Actions 工作流改为 Docker Compose 部署。

### 验证

- `pnpm typecheck` ✅
- `pnpm lint` ✅ 0 errors / 33 warnings
- `pnpm build` ✅
- 线上首页 200 ✅
- 本地容器 200 ✅

### 下一步

进入 P1 体验优化阶段：`<img>` 迁移 `next/image`、搜索高亮、联系表单通知增强等。


---

## 修复：嵌入页重复弹出 Cookie 横幅（同日追加）

### 问题

用户反馈内嵌 Demo（iframe）和站点页面都弹出 Cookie 使用说明横幅，导致嵌入体验很差。

### 根因

`CookieConsent` 组件放在 `src/app/layout.tsx` 根布局中，导致 `(embed)` 路由组下的所有 iframe 页面也渲染了横幅。

### 修复

- 从 `src/app/layout.tsx` 移除 `<CookieConsent />`。
- 在 `src/app/(site)/layout.tsx` 中添加 `<CookieConsent />`，仅站点壳页面显示。

### 验证

- `pnpm -s typecheck` ✅
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅

> 部署：用户需要重新构建并部署容器。


---

## 增强：Google Analytics 4 埋点（同日追加）

### 问题/需求

用户询问当前站点 analytics 是否需要做埋点。

### 已实现

1. **自动 page_view 补全**
   - GA4 的 `send_page_view: true` 只会捕获初始页面加载。
   - Next.js App Router 的 `<Link>` 客户端跳转不会自动触发新的 `page_view`。
   - 在 `GoogleAnalytics` 组件中新增 `RouteTracker`，监听 `pathname` / `searchParams` 变化，手动补发 `page_view`。

2. **自定义事件工具**
   - 新增 `src/lib/analytics/events.ts`，提供 `trackEvent(eventName, params)`。
   - 只有 `window.gtag` 存在且用户同意分析 Cookie 时才会发送。

3. **首批埋点**
   - `play_like`：用户点击玩法“喜欢”时上报（含 `slug`）。
   - `search`：用户在顶部搜索框提交查询时上报（含 `query`）。

### 后续建议埋点

- `play_card_click`：首页/列表页点击玩法卡片。
- `demo_start` / `demo_restart` / `demo_fullscreen`：Demo 交互。
- `contact_submit`：联系表单提交。
- `mod_login`：版主登录成功。
- `ai_analyze` / `ai_generate_cover`：版主 AI 工具使用。

### 验证

- `pnpm -s typecheck` ✅
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅


---

## 新增：Cloudflare Web Analytics 支持（同日追加）

### 背景

用户询问 Cloudflare Web Analytics 是否能与 GA4 同时使用，以及 GA4 是否必须 Cookie 同意。

### 实现

1. **新增 `CloudflareAnalytics` 组件**
   - 读取 `NEXT_PUBLIC_CF_ANALYTICS_TOKEN`。
   - 渲染 Cloudflare Web Analytics beacon 脚本。
   - 无 token 时不渲染任何内容。

2. **集成到站点布局**
   - 在 `src/app/(site)/layout.tsx` 中加入 `<CloudflareAnalytics token={cfToken} />`。
   - 嵌入页（iframe）不加载，避免重复统计。

3. **更新 `.env.example`**
   - 新增 `NEXT_PUBLIC_CF_ANALYTICS_TOKEN=` 配置项。

### 关键结论

- **Cloudflare Web Analytics 可以与 GA4 同时使用**，两者互补。
- **Cloudflare Web Analytics 不需要 Cookie 同意横幅**（无 Cookie、隐私友好）。
- **GA4 默认使用 Cookie**，在 GDPR/ePrivacy 适用场景下需要用户同意；站点已通过 CookieConsent 组件处理。

### 验证

- `pnpm -s typecheck` ✅
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅


---

## 优化：简化 Docker 部署命令（同日追加）

### 问题

用户反馈不理解每次部署都要执行 `set -a && source .env.local && set +a`。

### 根因

`docker-compose.yml` 的 `build.args` 需要读取 `NEXT_PUBLIC_*` 变量做构建时替换。之前的写法要求先把 `.env.local` 导出到当前 shell 环境，再运行 `docker compose up --build`。

### 优化

改为使用 docker-compose 原生的 `--env-file` 参数：

```bash
docker compose --env-file .env.local up --build -d
```

- 不需要 `set -a` / `source` / `set +a`。
- `.env.local` 同时给构建参数和运行时 `env_file` 使用。
- 已同步更新 `doc/docker-deployment.md` 和 `.github/workflows/deploy.yml`。

### 验证

- `pnpm -s typecheck` ✅
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅


---

## 追加：搜索体验第一层优化（2026-06-21 晚间）

### 已完成

- **扩展 plays 搜索索引**
  - `src/lib/content/plays.ts` 新增 `PlaySearchDoc` 类型、`stripMdx()` 与 `buildPlaySearchText()`。
  - 搜索字段从原来的 `title/subtitle/tags/techStack/corePoints` 扩展到：
    - `breakdown[].title` 与 `breakdown[].bullets`
    - `codeSnippets[].title`
    - `demo.note`
    - `article.mdx` 正文（去除 MDX 标记后截断 8KB）
  - 新增 `listPlaySearchIndex()` 供首页搜索使用。

- **统一搜索匹配与排序**
  - 新建 `src/lib/search/match.ts`。
  - 支持按空格/逗号/中文逗号分词。
  - 多 token 命中越多排名越高；标题命中权重 ×3，副标题命中权重 ×2。
  - 定义 `POPULAR_SEARCH_TERMS` 热门搜索词。

- **搜索结果高亮**
  - 新建 `src/lib/search/highlight.tsx`。
  - `highlightText()` 按 token 用 `<mark class="bg-highlight-yellow/60">` 高亮，避免 `dangerouslySetInnerHTML`。
  - `PlayCard` 增加 `highlightQuery` prop，标题/副标题/标签均支持高亮。

- **顶部搜索框建议**
  - 新建 `src/components/search/SearchSuggestions.tsx`。
  - 搜索框聚焦时显示热门搜索词下拉；输入时过滤相关建议。
  - 支持键盘上下选择、Enter 选中、Esc 关闭、点击外部关闭。
  - 使用 `SketchBorder` 保持手绘风格。

- **首页搜索与空状态**
  - `src/app/(site)/page.tsx` 改用 `searchPlayDocs()` + `filterPlaysBySearchResults()` + `sortPlaysBySearchResults()`。
  - 保留现有分类/难度/pattern 筛选叠加逻辑。
  - 无结果时显示热门搜索词快速链接 + 最新文章推荐。

### 验证

- `pnpm -s typecheck` ✅ 通过
- `pnpm -s lint` ✅ 0 errors / 33 warnings
- `pnpm -s build` ✅ 通过
- 提交 `c2efd84` 已推送到 `origin main`

### 关键决策

- 暂不把 archetypes/patterns/features 纳入全局搜索（P2 预留），第一期先把 plays 搜索体验做扎实。
- 暂不引入 `fuse.js` 等模糊搜索库，先用多 token 加权子串匹配；内容量小、实现简单、结果可解释。
- `article.mdx` 在搜索时实时读取，当前内容量可控；未来内容增长后改为构建时生成索引。
