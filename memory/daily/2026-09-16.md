# 2026-09-16

## 多语言 M1 落地：next-intl（zh-CN 默认 + en /en 前缀）

- **背景**：英文内容 32/33 篇早已备齐（`content/plays-en/`）但站点零引用、无英文路由（BACKLOG 里标注的下一里程碑）。本次按任务书实施 M1，未提交 git。
- **基础设施**：`next-intl@4.14.5`；`src/i18n/{routing,request,navigation}.ts`（locales `["zh-CN","en"]`、`localePrefix:"as-needed"`、**`localeDetection:false`**——不按 cookie/Accept-Language 强跳，Google 不建议自动重定向）；`src/middleware.ts`（matcher 排除 `api|embed|referencecase|_next|.*\\..*`）；`messages/{zh-CN,en}.json`。
- **路由重构（多 root layout）**：`(site)` 组整体迁入 `src/app/[locale]/(site)/`；`(site)/api/*` 上移到 `src/app/api/`（URL 不变、不进 locale 段）；**删除根 `app/layout.tsx`**——Next 16 根 layout 拿不到子段 params，改为 `[locale]/layout.tsx`（html lang SSR 正确 + NextIntlClientProvider + 原根 metadata/hreflang 默认）与 `(embed)/layout.tsx`（独立 root layout，固定 zh-CN）双 root layout；`error.tsx`/`not-found.tsx` 移入 `[locale]/`，新增 `[locale]/[...rest]/page.tsx` catch-all 保住手绘 404。
- **内容层**：`plays.ts` 全部读取函数加 `locale` 参数（默认 zh-CN 行为不变，embed/api 调用方零改动）；en → `content/plays-en`，meta 或 article 缺稿回退中文并置 `untranslated:true`；`PlayDifficulty` 放宽英文值（Beginner/Advanced/Hardcore）。
- **UI 串**：导航/抽屉/页脚/首页区块/文章页固定文案/搜索建议/Cookie 横幅/收藏页全部入 messages；站点组件 `next/link`→`@/i18n/navigation` Link（含 mod 页），`PlayCard`/`FavoriteButton` 等客户端组件用 `useTranslations`，服务端组件用 `getTranslations`/`getLocale`。
- **语言切换器**：`LanguageSwitcher.tsx`（中 / EN，当前语言下划线高亮，Link 保当前路径+query 换 locale，Suspense 包裹 useSearchParams）。
- **SEO**：play 详情 `generateMetadata` 输出 `alternates.canonical`（locale 感知）+ `languages`（zh-CN/en/x-default 互链）+ og:locale；archetypes/patterns/features [key] 页补同款 languages + 双语 generateStaticParams；`sitemap.ts` 每条 URL 输出双语 `alternates.languages`；首页 generateMetadata 补 canonical+/en 切换。
- **验证**：typecheck 零报错、build 成功；dev :19601 Playwright 27/27（中英首页/帖、en 回退+未翻译标记、切换器双向路径互换、hreflang 三标签+canonical、en 卡片链接带 /en 前缀、cookie=en 不强跳、embed/api 200）；console 零错误；404 三路径（未匹配 URL/坏 slug/非法 locale）均渲染手绘 404。截图 /tmp/i18n-{zh-home,en-home,en-play,switcher}.png。
- **遗留（M2）**：archetypes/patterns/features 页正文 spec 数据仍中文（content/archetypes 等无 -en 目录）；分类 Tab 标签（消除/合成等分类数据）与 POPULAR_SEARCH_TERMS 中文；about/privacy/terms/contact 正文中文；这些页面的 hreflang 暂用根默认（指向 /），应补页面级 alternates；en 难度浏览组筛选无效（difficulty 分类值为中文）；HotPlaysSection 已兼容 en "Hot" 标签。
- **部署注意**：生产 :19600 pm2 实例未动，仍跑旧构建；按流程需 `pnpm build` + 重启（或 release.sh 发布）才生效。standalone 产物 public 变更需 rsync 进 `.next/standalone/public`。
