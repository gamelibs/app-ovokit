# 2026-09-22

- 将 `match-clear` 母型页从概念提纲扩写为可实现、可验证的消除玩法指南：补齐交换回滚、批量匹配收集、连锁结算、重力补充、合法步检测、受约束重洗、质量指标与五项 Demo 实验。
- 显式将 2048 变体、连线消除、Match-3 三篇案例归属为 `match-clear`；补强 Match-3 文章的一次交换结算契约、TypeScript 骨架、单变量实验与六项玩法质量指标。
- 应用户要求收紧范围为「只做消除页面」：将 `content/archetypes-en/match-clear/meta.json` 同步升级到与中文版同等深度（结算顺序、死局重洗、5 项 demoLab 实验），消除中英文内容质量落差。
- **反向调整（同日晚）**：用户反馈上一版偏工程/算法化，新手看不懂，且首屏一句话定义太抽象。已将 `content/archetypes/match-clear` 与 `content/archetypes-en/match-clear` 全部重写为零基础可读的说法：subtitle 改为具体生活化描述（对照糖果消消乐/连连看），去掉扫描顺序、resolveCascade、二维数组等工程术语，demoLab 由 5 项精简回 3 项（去掉回放实验、合法步质量实验这类 QA 向内容）。
- **混合版（同日晚，第三次调整）**：用户要求"保留之前的，混合增加优化"，不是二选一。做法：前台内容（subtitle/problemsSolved/minimalRules/learningGoals/systemLoopHint/combos）保持零基础说法不变；把此前的深度工程内容合并回默认收起的"高级设计"折叠区——`advancedWarnings` 从 4 条扩到 5 条（每条先给结论，再补验证/判断方法）、`advancedAlgoRefs` 从 3 条扩到 6 条（补回棋盘数据独立于画面、按轮结算连锁等实现要点）；`demoLab` 从 3 项恢复到 5 项（补回规则回放实验、合法步质量实验，用更口语化的说法解释）。
- 验证：内容 JSON 解析通过；zh/en 字段与 demoLab 数量校验通过；`pnpm -s typecheck` 通过。
- 未修改页面组件；扩写内容由既有 `ArchetypePage` 与文章渲染器直接渲染，深度内容位于默认折叠的「高级设计」区块，不影响首屏新手可读性。
