# 2026-09-05 工作笔记

> 会话目标：gameslog.top 部署准备——创建部署分支、编写服务器侧一键部署脚本。
> 追加：同日下午按用户指令改为**产物型部署分支**模型（orphan 分支只含 standalone 产物）。

---

## 已完成（下午追加：产物型部署分支重建）

### 4. 分支归位与模型切换
- main 快进收编 deploy/gameslog.top 的 2 个提交（bf246dc、30a7921），旧源码型 deploy 分支删除。
- `next.config.ts` 加 `output: 'standalone'`（main 提交 5aae0fe）。
- 源码模式 `scripts/deploy.sh` 删除，package.json `"deploy"` 换成 `"release"`（main 提交 2d3e633）。

### 5. 新部署模型（源码在 main，产物在 deploy/gameslog.top）
- **Mac 侧 `scripts/release.sh`**（`pnpm release`）：校验（main 分支/工作区干净/.env.local 且 NEXT_PUBLIC_SITE_URL 为 https 真实域名）→ `pnpm install --frozen-lockfile` + `pnpm build` → 组装产物（`.next/standalone` 全量 + 手动补 `.next/static` 与 `public/` + `deploy/deploy.sh`、`deploy/ecosystem.config.js`、`deploy/.env.example`、`.gitignore`）→ git worktree 发布为 deploy 分支提交（首次 orphan 根提交，之后快进追加；信息含 main 短哈希 + 时间）。不执行 push。
- **服务器侧产物版 `deploy/deploy.sh`**（产物根）：`deploy`（update→check→start）/ `update`（ff-only）/ `check`（node≥20、pm2、.env.local 必填项与权限、端口）/ `start`（pm2 start 或 reload 零停机 + 30s 健康验证）/ `stop` / `restart` / `status` / `version`；deploy 成功写 `.deploy-meta`。
- **产物版 `deploy/ecosystem.config.js`**：`node server.js`，name `ovoforge-web`，NODE_ENV=production、PORT=13100。
- `scripts/deploy-check.sh`、`ecosystem.web.config.js` 保留在 main 作全源码部署参考，不进产物分支。

### 6. 首次真实发布与验证
- `bash scripts/release.sh`：构建 BUILD_ID `JDVdjr2xKiJXK9R0kKPlm`，产物 104MB，orphan 分支首个提交 `6c53f5e`（`产物：main 2d3e633（2026-09-05T10:39:09Z）`），总耗时 32s。
- **产物独立运行验证**（git archive 解到 /tmp/gameslog-artifact-test，测试 .env.local，裸 `PORT=13199 node server.js`，非 PM2）：首页 200（title/description/canonical=https://gameslog.top 正确）、内容页 `/play/2048-variant-probability-control` 200（content/ 运行时读取被 standalone 正确 trace，产物含 content/ 101 文件、games/ 66 文件）、`/_next/static/css` 200、字体 woff2 200、`/_next/image` 优化 200、public 直出 200、sitemap 输出 gameslog.top。验证后进程已杀、临时目录已删。
- deploy 分支历史干净（单个 orphan 提交，无源码历史）；非 node_modules 文件 1100 个；仓库 size-pack 71.61 MiB。

### 7. 环境发现（非本次操作造成）
- 验证时发现本机 PM2 进程列表为空、ovo_system gateway(:19527)/admin-web(:12048)/站点(:19600) 均不可达——整套本地服务在外部事件后未运行。本次任务未依赖它们（产物验证走裸 node 临时端口）。按任务规则未动 ovo_system 服务，如实上报。

## 已完成（上午：源码型部署准备，已被产物型取代）

### 1. 部署分支 `deploy/gameslog.top`
- 从 main HEAD（247b2dc）创建；首个提交 bf246dc 收编 2026-09-02/09-03 全部工作区修改（SEO/性能/触控修复、品牌收编、memory、`ovo-site.json` 契约、`public/icons/pattern-narrative.webp`），共 15 文件。
- `.env.local` 未被跟踪（gitignore 已覆盖），确认未入库；未 push（留给用户决定）。

### 2. `scripts/deploy.sh`（服务器侧一键部署）
- 子命令：`deploy [ref]`（update→check→build→start→健康验证全流程）/ `update [ref]` / `check`（直接调 `scripts/deploy-check.sh`）/ `build` / `start` / `stop` / `restart` / `status` / `version`。
- 要点：`set -euo pipefail`；check 失败中止 deploy；update 遇本地改动先 stash 提示、`git pull --ff-only`（不能 ff 报错人工介入）；start 已运行时用 `pm2 reload` 零停机；start/restart 后 30s 循环 curl 健康验证，失败输出 `pm2 logs --lines 50` 并 exit 1；deploy 成功写 `.deploy-meta`（已入 .gitignore）。
- package.json 新增 `"deploy": "bash scripts/deploy.sh"`。
- 踩坑：macOS 自带 bash 3.2 下 `$VAR` 后紧跟全角字符（如 `（`）会被并入变量名导致 unbound variable，已全部改为 `${VAR}` 花括号形式（12 处）。

### 3. 本地验证（未惊动本地运行环境）
- `bash -n` 语法检查通过；`version` exit 0（分支/commit/BUILD_ID 输出正确）。
- `check` 真实运行：脚本机制正常（正确调用 deploy-check.sh 并传播 exit 1），本地报 5 个错误均为本地开发环境事实（.env.local 权限 644、UPSTASH_REDIS_* 未配置、MOD_PASSWORD 为本地弱口令），属预期。
- 未在本地运行 start/stop/restart/deploy/update（本地实例由 ovo_system 以 PM2 `ovo-site-becf127c` 托管在 :19600；本脚本面向服务器 :13100 的 `ovoforge-web`）。

## 后续（待用户/服务器侧）

- 用户决定后 push：`git push -u origin deploy/gameslog.top`。
- 服务器首部署：clone → 配 `.env.local`（NEXT_PUBLIC_SITE_URL=https://gameslog.top、强 MOD_PASSWORD、UPSTASH_REDIS_*）→ `chmod 600 .env.local` → `bash scripts/deploy.sh deploy` → Nginx 反代 127.0.0.1:13100 + SSL。
- DNS：gameslog.top 解析到服务器后，走 ovo_system 上线复审（G/H 工段 online 节点复验）。
