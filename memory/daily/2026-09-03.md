# 2026-09-03 工作笔记

> 会话目标：修复 ovo_system 上线审查（siteId `becf127c`，域名 gameslog.top）中 3 个本地可修的 failed 节点。

---

## 已完成

### 1. d-seo-full（SEO 基础完整）✅ 修复
- 检查器口径（ovo_system `adsense-v1.json`）：title 10~60 字、description 50~160 字、keywords ≥5 字、canonical 必需、og:title 必需、禁止 noindex。
- `src/app/layout.tsx`：description 48 → 99 字；新增 `keywords`；新增 `metadataBase: new URL(getSiteOrigin())`。
- `src/app/(site)/page.tsx`：新增 `metadata.alternates.canonical = "/"`（只放首页；放根 layout 会让全站页面 canonical 都指向首页，反而伤害收录）。
- `.env.local`：`NEXT_PUBLIC_SITE_URL` → `https://gameslog.top`（sitemap/robots/canonical/OG 图 URL 同源生效，已验证 sitemap.xml 输出 gameslog.top）。

### 2. e-load-time（首屏加载性能）✅ 修复
- 根因：站点以 **dev 模式**运行（PM2 `npm run dev`），dev  bundle 未压缩，`main-app.js` 一个文件 transferSize 2.5MB，脚本合计 3.04MB，超过 3MB 上限。图片/字体都不是大头。
- 修复：`pnpm build` 生产构建；ovo_system 站点记录 `launch.script` dev → `start`（经 `PUT /api/site/:id`），launch-stop + launch 重启为 `npm run start`（与本仓库 `ecosystem.web.config.js` 的生产约定一致）。
- 结果：传输 3.32MB → **0.42MB**，load 646ms → 94ms。
- ⚠️ 注意：站点现在是生产模式，**代码修改不再热更新**，改完需 `pnpm build` 后经 ovo_system launch-stop/launch 重启。

### 3. e-mobile-viewport（移动端点击目标）✅ 修复
- 检查器口径：390×844 视口，抽样 `button/[role=button]/.btn/.button/input[submit]` 前 15 个可见元素，全部 ≥44×44px。
- `src/components/site/TopNav.tsx`：logo 按钮加 `py-2`（60×28 → 60×46）；搜索输入框 `h-9/min-[360px]:h-10` → `h-11`，搜索按钮 `h-8 w-8` → `h-11 w-11`（36×36 → 50×50），输入框右内边距改 `pr-[52px]`。
- `src/components/cookie/CookieConsent.tsx`：「仅必要」「同意全部」SketchButton 传 `className="inline-flex min-h-11 min-w-11 items-center justify-center"`（83×41/99×41 → 83×50/99×50，视觉不变，可点区域达标）。
- 已知遗留：`SketchButton` 默认高度约 41px（内层 `py-2 text-sm`），全站其它页面的 SketchButton 在严格 44px 口径下均偏小——本次只修首页抽样范围，全局触控尺寸治理建议立项（P1）。

## 审查结果（POST /api/site/becf127c…/launch-review 真实重跑）

- 总分 81 → **88**，passed=true，分布 passed 23 / failed 3 / pending-online 6 / manual 3。
- 3 个目标节点全部转 passed。
- 剩余 failed：`f-ads-txt`、`f-adsense-script`（AdSense 接入，本次范围外）。
- ⚠️ **d-responsive 假失败（检查器口径问题，非站点问题）**：检查器只抓第一个 `<link rel="stylesheet">`；生产模式下 Next.js 把 next/font 的 @font-face CSS（4KB，无 @media）排在 Tailwind 主 CSS（57KB，含 15 个 @media）之前，导致误判。dev 模式下首个样式表就是 Tailwind，故此前通过。站点响应式样式真实存在。已向 ovo_system 提交证据，建议检查器改为遍历全部外链样式表（属修正采样 bug，非放宽标准）。

## 验证

- `pnpm build` 两次均 exit 0。
- 检查器同口径自测：description 99 字 / keywords 41 字 / canonical 有 / 5 个触控目标全 ≥44px / 生产传输 0.42MB。
- Playwright 桌面 1600×950 + 移动 390×844 首页截图：手绘风格、布局、Cookie 横幅均正常，无横向溢出。
