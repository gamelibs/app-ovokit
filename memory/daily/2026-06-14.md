# OVOFORGE 工作笔记 — 2026-06-14

## 今日目标

1. 理解项目当前状态与用户需求
2. 建立 Agent 工作流记忆系统
3. 确认"5 种核心玩法原型"的实施方向
4. 完成迭代 1：核心原型数据层 + 筛选 + 版主表单
5. 完成迭代 3：5 种核心原型最小 Demo + 嵌入页面 + 详情页回退

## 已完成

- 读取项目核心文档：`README.md`、`doc/AGENT_CONTEXT.md`、`doc/开发说明.md`
- 检查代码库状态：
  - `pnpm typecheck` ✅ 通过
  - `pnpm build` ✅ 通过
  - `pnpm lint` ❌ 115 errors / 1749 warnings
- 确认核心模块接口：
  - 母型玩法：`src/lib/archetypes/*`
  - 内容模型：`src/lib/content/plays.ts`
  - 版主表单：`src/components/mod/NewPlayForm.tsx`
  - Demo 体系：`server/demos/*`
- 建立 Agent 工作流记忆系统：
  - `doc/AGENT_WORKFLOW.md`
  - `memory/STATUS.md`
  - `memory/GOALS.md`
  - `memory/BACKLOG.md`
  - `memory/DECISIONS.md`
  - `memory/daily/2026-06-14.md`
- 更新 `AGENTS.md` 和 `doc/AGENT_CONTEXT.md`，把 `memory/` 加入默认阅读顺序
- 与用户确认实施方案：5 种核心原型作为与 12 母型并存的第二层分类
- **完成迭代 1**：
  - 新增 `src/lib/patterns/patterns.ts`（5 种核心原型定义）
  - 扩展 `PlayMeta` 增加 `pattern` 字段
  - 首页筛选新增"核心原型"分组，支持按 `pattern` 过滤
  - 改造 `NewPlayForm`，版主发帖可选择核心原型
  - mod API 自动透传 `pattern`，无需额外修改
  - 修复 `isPlayBrowseGroupKey` 缺少 `"pattern"` 的判定
- **完成迭代 3**：
  - 新增 5 个核心原型服务端 Demo：
    - `pattern-action`：跳跃躲避
    - `pattern-spatial`：3×3 十字翻转
    - `pattern-merge`：资源合并升级
    - `pattern-management`：建造与产出
    - `pattern-strategy`：回合制战斗
  - 新增嵌入页面 `/embed/demos/pattern/[key]`
  - 注册到 `server/demos/registry.ts`
  - 详情页 Demo 回退逻辑优先使用帖子的 `pattern`
  - 版主表单选择原型后自动推荐默认 iframe 地址
- 验证：
  - `pnpm typecheck` ✅
  - `pnpm build` ✅
  - API init/step 测试通过（spatial / action / management）
  - 5 个嵌入页面均返回 200
  - 根据 tags 自动推断 pattern：strategy 9 篇 / action 6 篇 / spatial 10 篇 / merge 5 篇 / management 0 篇
- **Demo 体验优化**：
  - 重写 `ServerDemoPlayer`：手绘风格、自适应 flex 布局、自动添加 tick 推进按钮、更大控制按钮、事件区可滚动
  - 改进嵌入页面：去掉深色背景，改为 `bg-paper`
  - 改进 `DemoEmbed`：默认高度从 `h-[68vh] aspect-video` 改为 `min-h-[360px] aspect-[4/3]`，更适合 ServerDemoPlayer

## 关键决策

- 5 种核心原型：action / spatial / merge / management / strategy
- 与 12 母型并存，一个帖子可同时属于 1 母型 + 1 原型 + N 标签
- 分 3 个迭代推进：数据层（完成）→ 母型映射展示 → Demo 模板（完成）

## 下一步

1. 提交迭代 3 的代码改动
2. 开始迭代 2：母型与核心原型的映射 + 母型详情页展示
3. 或修复 `useBlockEditor.ts` 的 `commit` 提前访问 bug

## 阻塞

- 无。
