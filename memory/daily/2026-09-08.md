# 2026-09-08 工作日志

## SEO 基础修复（P0-2）完成

- **双 H1 修复**：`ArticleMarkdown.tsx` 的 `Heading` 组件将 MDX 标题整体降一级渲染（`#` → h2），文档 H1 只由页面标题卡提供。此前 play 页 MDX 首个 `# 标题` 与页面标题重复产生两个 H1。
- **品牌后缀收编**：play 详情页 title/og:siteName、favorites 页 title/description、contact 邮件表单称呼、generate-article 落款，共 5 处硬编码 `OVO` 全部改走 `siteConfig.name`（GamesLog）。全仓扫描确认无其它用户可见 OVO 残留（OvoLogo 为内部组件名，无可见文字）。
- **hreflang 暂缓**：当前无任何英文路由（`content/plays-en` 有 1 篇内容但无渲染入口），hreflang 无的放矢，并入英文化任务（P0-1）一并落地。
- **验证**（:19600 生产实例，build 后经站点管理 API 重启）：play 页 H1=1、title `… - GamesLog`、MDX 标题渲染为 h2、favorites title 正确、首页 200 回归正常。抽查 tic-tac-toe-showdown 同样单 H1。
- **注意**：以上改动仅生效于本地生产实例；线上 gameslog.top 需走 `pnpm release` 产物发布流程（tic-tac-toe demo 文章的 localhost iframe 阻塞未解，发布前需先处理）。
- **相关决策**：品牌露出规则见 ovo_system `docs/brand-architecture.md` 与本仓 `memory/DECISIONS.md` 决策 005。

## 关联进展（ovo_system 侧，同日）

- pixelbook（ovoforge.com，工具站）已绑定进站点管理；站点类型新增 `tool`；预览/启动/审查三条链路对 external 站点的通用性缺陷已修复（详见 ovo_system DECISION_LOG）。

## 存量内容反推审查（首跑）

- 新增 `scripts/audit-content.ts`（用法 `npx tsx scripts/audit-content.ts`），三层审查：A 数据层 / B 渲染层（:19600 实例）/ C 内链层；阈值与管线门禁口径对齐（正文 ≥800、breakdown ≥2、description 50~160），taxonomy/tag 映射读 ovo_system 真相源。报告落盘 `memory/audits/audit-2026-09-08.json`。
- 首跑结果：32 篇 + 14 母型页，共 **102 问题**（content-fix 15 / pipeline-gap 85 / blocked-demo 2 / site-bug 0）。
- 主要问题类：subtitle 过短 ×30（数据层+渲染层同源）、正文 <800 字 ×15、无母型归属 ×9、母型空集群 ×7/14、demo 资源 404 ×6、英文覆盖 1/32、localhost demo ×2（tic-tac-toe-3d-rotation-juice 等）。
- 体检结论：门禁规则本身有效（存量问题全是既有门禁会拦的，属门禁建立前的历史存量）；真实管线缺口 = ① 无存量再审机制（本脚本补上）② 无 demo/封面资源校验门禁 ③ tag 映射治理缺失。
