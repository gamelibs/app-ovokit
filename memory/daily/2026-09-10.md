
## 内容生产线渠道上下文化（ovo_system 侧改造，本站仅同步文档）

- ovo_system 内容生产页新增页面级「站点/渠道上下文」选择器（?channel= 深链），选题/草稿/发布/审查按渠道过滤；站点管理卡片新增「内容生产」入口反查 siteRef 跳转。
- 后端：drafts 表加 channel_id（空=全局草稿，创建时从选题 payload.channelId 继承或上下文指定）；topics 按 payload.channelId 过滤；存量草稿已回填（发布草稿按 primaryLang 匹配渠道语言归 gameslog-main，测试草稿保持全局）。
- 本站改动仅 `doc/站点内容生产线说明.md` 同步新增 §5.4（对应权威版 ovo_system docs/site-content-production-line.md v1.3）。审计文档：ovo_system docs/content-pipeline-adapter-contract-audit.md（第二个站点接入前必读）。

## 审查器新增 3 条机检 + tic-tac-toe-3d 精修（下午）

- **新基线 320→314**：审查器（本站 scripts/audit-content.ts 与 ovo_system D 层适配器同口径）新增 3 条机检——`title-jargon`（标题黑话黑名单，初值「果汁」）、`en-meta-cjk`（英文版 meta 中文残留：tags/difficulty/demo.note）、`en-description-length`（EN subtitle 50~160）。补检后全量基线 255→320（en-meta-cjk ×32、en-description-length ×32、title-jargon ×1）。
- **tic-tac-toe-3d-rotation-juice 精修完成**：双语标题去「果汁」/去 Quantum Grid 品牌（zh「规则不变也能翻新体验：井字棋 3D 化的表现层改造与 Minimax 复用」/ EN "Same Rules, New Feel: A 3D Makeover for Solved Tic-Tac-Toe"）；修摘要事实错误「无解」→「早已被完整解决」（原表述说反，且会进 SERP description）；zh 正文修「20 世纪初」不可考表述、Hook 章节改名、术语统一「游戏感（juice）」；EN meta 中文残留清零（tags→Turn-Based/Board/Strategy、difficulty→Beginner、demo.note 英文化）、EN 正文修 "the 'juice' (juice)" 冗余；程序化封面补齐（cover.svg/cover-wide.svg，meta 已回写）。
- **管线草稿已同步**：draft_1788754464316_ikzzod 经 PUT API 更新双语 title/summary/bodyMarkdown/channelMeta，en 译文 fresh——防止将来从管线重发布覆盖本次修复。
- 剩余 6 个问题 = demo-localhost（红线，等静态导出）+ D 层 5 项（批 3/批 4 范围）。
- **两个坑**：① :19600 生产实例（next start）对 public/ 快照于进程启动时——新增 public 资源必须 `pm2 restart ovo-site-becf127c` 才可达（ISR 只管 content/）；② 审查脚本输出不能管道给 `head`（SIGPIPE 会杀死进程导致报告不落盘）。

## 存量反推导入完成（下午收尾）

- ovo_system 新增 `scripts/content-pipeline/import-inventory.ts`：把本站 content/plays（+plays-en）32 篇倒装进管线 DB，管线成为唯一主库。幂等——首跑「新建 31 + 刷新 1（tic-tac-toe-3d）+ 登记 pack zh=31/en=31」，补 archetype 推断兜底后重跑 32 篇全走刷新路径。
- **保真关键点**：archetype_refs = 显式 meta.archetype 优先，否则按 taxonomy/mappings/site-tags.v1.json 从 tags 推断（与站点/审查器同口径）；否则将来从管线重发布会丢 archetype、tags 被重算。兜底后 32 篇 published 草稿 archetype_refs 全非空。
- 字段映射：subtitle→summary/seo.description；正文剥首个 H1 入库（发布时 ensureArticle 补回）；difficulty/techStack/corePoints/breakdown/codeSnippets/cover/coverWide/demo/published→channelMeta；stats 发布时从站点旧 meta 继承；英文版→translations.en（fresh）。
- 验证：API drafts?channelId=gameslog-main=36 条；草稿审核台 UI 渠道过滤正常显示 30+ 条（截图 ovo_system tmp/cp-drafts-scoped.png）。
- 权威文档：ovo_system docs/site-content-production-line.md 新增 §13（v1.4）。
- 导入只写 DB 不回写站点文件；红线不变——demo-localhost 未解决前禁止 pnpm release。

## tic-tac-toe-3d 深度修复 + demo localhost 渲染层守卫（傍晚）

- **渲染层守卫**：`src/app/(site)/play/[slug]/page.tsx` 新增 `isPrivateDemoSrc()`——demo 地址为 localhost/127.*/192.168.*/10.*/172.16-31.* 时不渲染 iframe/video，改渲染「公开试玩准备中」占位卡（note 保留）。一处改动全站免疫；meta 地址不动、审查器 demo-localhost 继续追踪，静态 demo 导出落地后守卫自动失效。已 build + pm2 restart ovo-site-becf127c 验证：页面 200、HTML 无 localhost 残留、占位文案渲染正常。
- **文章精修第二轮**（双语同步）：① 正文补状态机段落（menu→gameplay→result→重开/回菜单，兑现 meta breakdown 承诺）；② 新增「数据模型」小节（GameState 最小结构，正文首个代码块）；③ 「关键实现：minimax 为什么不可战胜」挂真实证据——codeSnippets 新增第二段，摘录自 ovo_system modules/core/board-ai/logic.ts 的 minimax（重建版真正复用的实现）；④ 新增「可迁移的玩法原语」小节（4 条）；⑤ 三个章节名对齐指南 §四 标准结构（玩家实际经历/核心机制拆解/关键实现）；⑥ 标点统一（直角引号→弯引号、U+2011→连字符）；⑦ EN snippet 1 中文代码注释英文化。
- **审查基线 314→309**，本篇 6→1（仅剩 demo-localhost，已守卫+追踪）。发现审查器机检只匹配 H1~H3 章节标题，「玩家实际体验」不含连续子串「玩家体验」/「实际经历」，需用指南标准名。
- **确认事实**：plays-en 只被审查器/翻译脚本/管线 gameslog-en 渠道消费，站点暂无 /en/ 路由——EN 内容是为未来 i18n 与管线备料，无 EN 页面可验证属预期。
- 管线 DB 已重跑 import-inventory.ts 同步（32 篇全刷新）。
