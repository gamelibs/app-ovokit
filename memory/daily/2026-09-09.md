# 2026-09-09 工作日志

## 存量审查 UI 化（ovo_system 侧，本仓受益）

- 存量审查从 CLI 脚本搬进 ovo_system 系统管理「内容生产」页第五个 Tab「存量审查」。
- 实现路径：`gameslog-mdx` 适配器新增可选能力 `auditInventory`（与 `scripts/audit-content.ts` 同口径三层审查）；引擎 `runInventoryAudit` 落库新表 `inventory_audit_reports`；路由 `POST /audit/run`、`GET /audit/latest`、`GET /audit/reports`；baseUrl 由 controller 从站点注册表（siteRef → serveUrl/localUrl/domain）自动解析，可显式覆盖。
- title 后缀校验的站点名不再硬编码，改读本仓 `src/lib/site/config.ts`。
- 验证：API 与 UI 各跑一遍，均与 CLI 基线一致（32 篇 / 102 问题）。

## 指南机检化（D 层）+ 批 1 母型归属治理（已完成）

- **审查器新增 D 指南结构层**（`scripts/audit-content.ts` 与 ovo_system 适配器同步实现，两端结果一致）：机检《生产内容注意事项》§四/§七——9 个标准章节存在性 / 正文代码块 ≥1 / 重复句子凑长度 / meta.demo 存在性。首跑 D 层 334 个缺口：guide-experience/mechanics/edge-cases/tradeoffs ×32，essence/implementation/code ×31，loop/data-model/primitives ×30，demo-missing ×24。全量 377 问题（A/C 存量 32 + D 334 + blocked-demo 2）。
- **批 1 母型归属治理完成**：9 篇无归属文章按内容忠实原则补显式 `meta.archetype`——merge-unit ×3（merge-chain-and-gates、merge-level-core-loop、play-1677a241）、progression ×6（clicker/drag-idle/gacha/infinite-upgrades/meta-progression/random-events）。依据：site-tags 映射中 合成/点击/放置/数值/Roguelike 均为 feature 级 tag（故意不参与推断），「合成」条目的 note 明确「若为主循环则 archetype=merge-unit」。备份 `memory/audit-fix-backups/2026-09-09/*/meta.before-archetype.json`。复审：archetype-unattributed ×9 清零，pillar-empty 7→**5**（runner/shoot-aim/choice-strategy/timing/simulation 需新内容，进选题反哺）。
- **发现站点功能缺口并修复**：母型页只有 article→pillar 单向回链，无 pillar→cluster 案例列表。已在 `pageModel.ts` 加 `relatedPlays`（显式 archetype 优先 / tag 推断兜底）、`ArchetypePage.tsx` 加「⑥ 相关案例文章」区块（空集群时不渲染）。build + 重启 :19600 验证：merge-unit 页列出 3 篇、progression 页 6 篇、runner 页无区块（正确）；全站回归 site-bug 0。
- 最终基线（2026-09-09）：**366 问题** = A/C 存量 32（body ×15、demo ×8、cover ×2、pillar-empty ×5、en ×1）+ D 指南结构 334。

## 批次 1 文案回刷（subtitle 完成 / body 待审）

- 新脚本 `scripts/fix-batch-1-text.ts`：扫描 content/plays 找出 subtitle 越界（50~160）与正文 <800 字的篇目，调 LLM 重写；默认 dry-run，`--write` 写回前自动备份到 `memory/audit-fix-backups/{date}/{slug}/`；`--dump` 把 dry-run 产出落盘供人工审查；校验不合格（超长/过短/丢章节/截断）判失败不写入。
- **模型偏差（重要）**：forge-studio（192.168.0.103，20B 主力）整天离线（ping 不通）。改用本机 omlx（127.0.0.1:1888）。GLM-5.2-mxfp4（19.94GB）超内存天花板（可用 8.41GB）加载被拒；最终用 `Qwen3.5-9B-MLX-4bit` + `/no_think` + `chat_template_kwargs.enable_thinking=false`（否则思考烧光 token 预算，77s/篇 → 3.9s/篇）。
- **subtitle 30 篇全部重写并写回**（5→10→15 三批，零失败），复审：102 → **42 问题**（description-length ×30 与 render-description-length ×30 全清零）。抽样核对无编造（gacha 篇「模拟验证」有原文依据）。
- 风格注意：原 subtitle 多为犀利口语但 <50 字；AI 版信息更全但偏 SEO 八股（高频「本文拆解/帮助开发者」）。若倾向保留原风格，可考虑把门禁下限从 50 调到 40 并回滚部分篇目（备份都在）。
- **body 15 篇 dry-run（未写回）**：首篇（2048-variant）发现 AI 扩写引入与原文矛盾的参数建议——原文写「棋盘接近满：提高低值（给合并空间）」，AI 新增段落写「空位 < 3 时生成 2 的概率从 90% 降至 70%」方向相反。结论：**正文批次必须人工逐篇审查 `tmp/batch1-review/` 后才能 --write**；9B 模型做扩写有事实漂移风险，如等得起可等 forge-studio 20B 恢复再做。

## 当前存量审查待办（42 问题）

- body-too-short ×15（批次 1 下半场，待人工审查）
- archetype-unattributed ×9 + pillar-empty ×7（批次 2 归属治理）
- demo-404 ×6 + demo-localhost ×2 + cover 缺失 ×2（批次 3 资源修复）
- en-coverage 1/32（批次 5 英文回刷）

## 批 2 指南结构补全（32/32 完成）+ 首页 UI 修复（下午）

- **新脚本 `scripts/fix-batch-2-guide-sections.ts`**：给每篇文章补「一句话本质 / 边界条件 / 设计取舍」三个指南标准章节（对应 D 层 guide-essence / edge-cases / tradeoffs 机检项）。设计要点：LLM 只生成章节正文，**插入位置程序化**（本质→H1 后；边界/取舍→文末「一句话总结」前，指南 §8→§9 顺序），原文一字不动；**反编造护栏**：生成文本的反引号标识符必须全部存在于原文，否则判失败；每篇最多 2 次尝试。
- **CRLF 坑（重要）**：content/plays 的 mdx 全是 CRLF 行尾，JS 正则的 `.` 不匹配 `\r`，`^(# .+\n)` 在 CRLF 文件上静默失效。修法：插入前统一转 LF、写完转回原行尾。批 1 脚本只改 meta.json 未受影响。
- **试点 5 篇 dump 人工审**（tmp/batch2-review/）：4/5 通过；fsm 篇被护栏拦下 `` `if/else` ``（原文无此标识符），重试后通过。漂移模式：无虚构数字/参数/API；残留轻微注水集中在「设计取舍」的代价/替代字段（通用设计常识，非事实错误），用户确认按此质量写回（选项 1）。
- **三批写回全成功**：5 + 10 + 17 = 32/32，零最终失败。备份 `memory/audit-fix-backups/2026-09-09/{slug}/article.mdx`（32 篇全）。
- **复审（audit-2026-09-09.json）：366 → 257 问题**。guide-essence 31→0、guide-edge-cases 32→0、guide-tradeoffs 32→0；附带收益 body-too-short 15→1（仅剩 roguelike-horde 791 字差 9 字）。剩余 D 层 240：experience/mechanics ×32、implementation/code ×31、loop/data-model/primitives ×30、demo ×24 → 批 3（玩法循环+玩家经历，需对照 demo）、批 4（数据模型+关键实现+代码块；注意 meta.codeSnippets 已有代码，可确定性搬运进正文，不一定需要 LLM）。
- **首页 UI 修复**：`RightSidebar.tsx`「新手必读」此前是写死的空色块（从不渲染封面），改为真实封面缩略图（sketch-border + object-contain，复用 PlayListItem 模式）；选品从 `slice(0,3)` 改为按「入门」难度筛选（名实相符，无封面跳过）。`play-1677a241` meta 清洗：标题 `小游戏玩法知识分享---之---合成` → `合成玩法：位置检索、类型判断与产出生成`（cover.alt 同步），techStack `js`→`JavaScript`，corePoints 逗号粘连拆分。typecheck 过，build + pm2 restart ovo-site-becf127c 后 Playwright 截图验证通过（tmp/sidebar-newbie.png）。

## 批 5 英文回刷（32/32 完成）+ 主页去重 + 正文栅栏修复（傍晚）

- **新脚本 `scripts/fix-batch-5-translate-en.ts`**：content/plays → content/plays-en 全量英译，本机 omlx 9B（`Qwen3.5-9B-MLX-4bit`）。每篇 2 次调用（meta 字段 + 正文 MDX）。翻译口径：title/subtitle/corePoints/breakdown/cover alt/codeSnippets 标题/正文（含标题）翻译；tags/difficulty/techStack/archetype/pattern/demo/stats/published 保留；新增 `lang:"en"`；代码逐字节保留。校验：meta 数组长度对齐 + 零 CJK 残留；正文标题数一致 + 代码块逐字节一致 + 非代码区 CJK <2% + 长度 ≥ 原文 40%。
- **9B JSON 不稳的教训**：meta 走 JSON 时偶发「丢 closing brace」等语法错误（fsm 篇连挂 4 次）。最终方案：**第 3 次尝试切纯文本分节格式**（[TITLE]/[BREAKDOWN] 段落），确定性解析，两篇顽固失败（fsm/grid-movement）一次通过。正文 max_tokens 2600。
- **play-1677a241 翻译反复失败的根因是中文原文 bug**：` ```ts ` 代码栅栏未闭合，批 2 加的「边界条件/设计取舍」实际落在代码块内（页面把它们渲染成代码文本）。已在原文补闭合栅栏并验证渲染恢复（h3 标题）。批 2/审查器的标题检测不排除代码区，是已知检测口径误差，暂不修。
- **结果**：32/32 英文版写回 plays-en（tic-tac-toe-3d 旧不合格样本已覆盖并备份）。复审 257 → **255**：en-coverage 清零，guide-code-missing 31→30（栅栏修复附带收益）。全站回归 site-bug 0。
- **主页「最新发布」封面去重**：封面按母型模板生成（31 张封面仅约 11 张不同，3 篇 merge 文章 MD5 逐字节相同），一行 3 张同图像重复内容。`LatestPlaysSection.tsx` 加同母型去重（显式 archetype 优先 + `inferArchetypeFromTags` 推断兜底——塔防两篇无显式 archetype 但封面同模板，靠推断拦住）。build + 重启后截图验证 5 张封面全不同。**根因未除**：cover-gen 需按文章差异化封面（backlog，可结合 pixelbook）。
- **已知遗留**：① EN subtitle 偏长（抽查 392 字符，超 SERP ~160 截断位），英文路由上线时统一修剪；② 「最新发布」mtime 排序在批量修复后已失效，正解是 meta.publishedAt（backlog，随英文路由/hreflang 里程碑）；③ 英文内容尚无路由（src 零引用 plays-en），**英文路由 + UI chrome 英文化 + hreflang 是下一里程碑**，不做完英文页面对 Google 不可见。
