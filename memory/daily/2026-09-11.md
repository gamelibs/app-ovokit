# 2026-09-11

## 静态 demo 导出落地（tic-tac-toe-3d 解除 demo-localhost 红线）

- ovo_system 新增 `scripts/export-static-demo.ts` + `buildV2PreviewHtml` 的 `staticExport` 模式：v2 项目预览完全静态化（kernel 本地拷贝 / 模块 dist JS / 全相对路径 / 数据 manifest），产物自包含、可放任意静态站点。
- 本站产物：`public/embed/plays/tic-tac-toe-3d-rotation-juice/`（index.html + kernel 24 文件 + modules 3 个 + ui/resources + export-manifest.json）。双语 meta `demo.iframeSrc` 改指站内路径，note 更新为「静态化导出、完全在站内运行」。
- 站点改动：`play/[slug]/page.tsx` 的 `demoOrientation` 增加 `/embed/plays/` → portrait 规则。
- 验证：独立静态服务器冒烟（boot→menu→1P→落子→AI 应手，零 console 错误）；:19600 实测 iframe 站内加载、无 pageerror；审查基线 309→308，`demo-localhost` 剩 tic-tac-toe-showdown 1 篇（待同款导出）。
- 管线 DB 已重跑 import-inventory.ts 同步（demo 字段刷新）。
- 权威文档：ovo_system docs/site-content-production-line.md §14（v1.5）。

## 内容生产管线新增「Demo 反推导出」入口（文章 → demo 闭环）

- ovo_system 草稿审核台新增「Demo 配置」区：iframeSrc + note + 状态标签（未配置/阻塞/正常）+「从 v2 项目反推导出」对话框。
- 链路：选 v2 创作项目 → 静态导出到本站 `public/embed/plays/{slug}/` → 回写草稿 `channelMeta.demo.iframeSrc`（保留旧 note，译文状态不受影响）。
- API：`GET /api/content-pipeline/demo-projects`、`POST /api/content-pipeline/drafts/:id/demo-export`；CLI 与管线共用 `services/static-demo-export.ts`。
- 顺手修复：草稿保存此前会丢 channelMeta 里的 cover/coverWide/demo/published（整字段覆盖 bug），已改先展开旧值。
- 验收：tic-tac-toe 草稿实跑幂等成功；UI 截图 tmp/cp-demo-*.png（ovo_system）。详见 ovo_system docs/site-content-production-line.md §14.1。

## Quantum Grid 3D demo 全链路跑通（创作中心工作流 → 站点文章内嵌可玩）

- **背景**：文章 `tic-tac-toe-3d-rotation-juice` 的正式游戏源是远程库 astrocade 站 slug `3d-rotating-tic-tac-toe`（真实 3D 旋转井字棋），此前 demo 是 2D 项目替身。本次按用户硬约定全程走系统工作流替换为 3D 重建版，我不手写 GameDefinition/blueprint。
- **A 能力建设（ovo_system）**：v2 此前零 3D 能力，本次补齐——
  - 新增 `modules/core/board-3d/`（Three.js 立体棋盘，契约与 core/board-turn 完全对齐：落子弹入 / 每回合绕 Y 转 90° / 胜方脉冲 / overlay canvas raycast 拾取）；
  - `module-registry.json` 注册；`v2-preview-html.ts` 支持 threejs 多运行时库顺序加载 + `ctx.engine.three`；
  - `game-definition-adapter.ts`：`normalizeEngine` 增加 frameworks 辅助信号（engine 字段写 "HTML5 Canvas" 但 frameworks 含 Three.js 时正确判 threejs）、board-game 3D 变体（跳过 grid-cell、棋盘区域放大）、菜单按钮识别补 play-ai/vs-ai、**修历史缺陷**（gameplay requiredTransitions 补 game.backToMenu→menu，2D 旧项目同受此 bug）；
  - `core/board-ai` 落子调用 board-turn ?? board-3d fallback。
- **B 工作流产出（全程系统真实入口）**：
  - 中途排障：上游 forge-studio oMLX 推理整体挂死，`POST /api/local-agent/omlx/restart`（需先 login 拿 cookie）恢复；
  - `POST /api/v2/game-sources/analyze`（remote/astrocade/3d-rotating-tic-tac-toe）→ job `job_1789120754118_rvx9w0`，识别 Quantum Grid / board-game / 4 态；
  - 用户确认流程图审查后 `create-project` → **cp_1789130311150**（engine=threejs）；旧 2D 项目 cp_1788757022723 保留作链路对照，孤儿项目已清理；
  - 主循环 e2e `tests/e2e/test-quantum-grid-3d-loop.mjs` 全过（menu→gameplay→真实 raycast 点击+AI 应手→result→restart→backToMenu，console 零错误）；
  - `POST /api/content-pipeline/drafts/draft_1788754464316_ikzzod/demo-export`（projectId=cp_1789130311150）→ 覆盖 `public/embed/plays/tic-tac-toe-3d-rotation-juice/` 并回写 demo.iframeSrc；
  - 草稿 channelMeta.source 补齐溯源字段（type=remote-game / site / slug / analysisJobId / projectId / engine=threejs / rebuiltAt）。
- **关键坑**：站点是 Next.js standalone 产物，pm2 `ovo-site-becf127c`（:19600）服务 `.next/standalone/public` 副本——public 变更需 `rsync -a --delete public/embed/plays/{slug}/ .next/standalone/public/embed/plays/{slug}/ && pm2 restart`，比 pnpm build 快且对纯 public 变更等效。
- **站点端到端验证**（Playwright，:19600 生产实例）：`/play/tic-tac-toe-3d-rotation-juice` 页面 iframe 内 3D demo 启动到 menu、1P 开局、真实鼠标点击落子 + AI 应手全过（截图 tmp/article-demo-final.png @ ovo_system）。坑：iframe 在首屏下方须 scrollIntoViewIfNeeded；3D 透视下点击取 (0.5, 0.42) 比例位置；选择器 `canvas[data-board3d]`。
- 文档：ovo_system `docs/DECISION_LOG.md` 追加「v2 补 3D 能力」决策；`AGENTS.md` 新增 §8.7「3D 棋盘变体 Recipe」。
- 已知 cosmetic 瑕疵（未修）：gameplay 皮肤有 AI 生成的占位文本显示在棋盘上方。
- **三仓库均有待提交改动**（ovo_system / ovo_workspace / gameslog-site），等用户确认后提交。
