# 2026-09-02 工作笔记

> 会话目标：站点域名从 ovoforge.com 切换为 gameslog.top（ovoforge.com 已转给其它站点使用），品牌收编为 GamesLog，仓库达到「品牌中性」。

---

## 已完成

### 1. 域名切换（ovo_system 侧驱动）
- ovo_system 站点记录（siteId `becf127c`）域名 ovoforge.com → gameslog.top，契约文件 `ovo-site.json` 同步回写（domain/localUrl + updatedAt）。
- gameslog.top 尚未部署/解析，系统产线「上线」阶段已正确退回未达标，后续走模拟上线流程再真实部署。

### 2. 品牌中性化收编（本仓库）
- `src/lib/site/config.ts`：站名 `OVO` → `GamesLog`；新增 `getSiteHost()` 辅助。
- `src/app/layout.tsx`：metadata 标题/siteName 改为引用 `siteConfig`，消除硬编码 "OVO"。
- `src/app/(site)/privacy/page.tsx`：硬编码「域名 ovoforge.com」改为 `{getSiteHost()}`。
- `src/app/(site)/api/contact/route.ts`：webhook fallback URL 改为 `siteConfig.url`。
- `docker-compose.yml` / `.env.example`：默认域名示例改为 gameslog.top。

### 3. 明确不动的部分（技术命名空间，与品牌无关）
- localStorage key（`ovoforge_favorites` 等）、自定义事件名、CSS 变量、`ovoforge_mod` cookie、Redis 前缀、package.json name、docker 容器/网络名——改动会破坏存量用户数据与部署脚本，永久保留。

## 验证

- `pnpm typecheck` 通过。
- 本地 dev（端口 19600，由 ovo_system PM2 托管启动）：首页 title 为「GamesLog - 游戏玩法分享与学习」；隐私页渲染「域名 localhost:13100」（生产注入 `NEXT_PUBLIC_SITE_URL=https://gameslog.top` 后即为 gameslog.top）。

## 后续

- 部署到 gameslog.top：DNS 解析 + 部署 + `NEXT_PUBLIC_SITE_URL=https://gameslog.top`。
- 批量多站点模式：品牌/域名均为数据（env 注入），禁止在源码中硬编码站点名/域名。
