# 2026-09-07

## 本轮：cluster→pillar 回链落地 + 补齐 merge-unit / turn-duel 母型页

### 做了什么

1. **tag 映射收编**
   - 新增 `src/lib/archetypes/tag-map.ts`：ovo_system `taxonomy/mappings/site-tags.v1.json`（ovo-taxonomy-mapping v1.0.0）的站点侧代码化拷贝，文件头注明来源与同步口径（映射只能在协议方修改，站点侧跟随）。
   - play 详情页删除 12 条硬编码 `tags.has(...)` if 链，改用 `inferArchetypeFromTags(play.tags)`。

2. **回链落地（SEO 权重回路）**
   - `src/app/(site)/play/[slug]/page.tsx` 新增「玩法行为」区块（order-1，标题卡之后、Demo 之前）：显示「本文属于「X」母型」+ 母型 subtitle + 「查看母型详解 →」链接到 `/archetypes/[key]`。
   - 样式复用本页既有 sketch 工具类（sketch-card / sketch-border / bg-highlight-yellow 令牌 / font-kalam），无写死颜色、无手写 border。

3. **补齐两个母型**
   - `src/lib/archetypes/archetypes.ts`：`playArchetypeKeys` + merge-unit / turn-duel；`archetypeToPatterns` 按 taxonomy defaultPattern 映射（merge-unit→merge、turn-duel→strategy）。sitemap、/archetypes 列表、/mod/archetypes、`listArchetypeSpecs` 全部自动生效。
   - `content/archetypes/{merge-unit,turn-duel}/meta.json`：字段结构对齐 match-clear/progression；内容严格取自 taxonomy.v1.json（definition/primitives/compatibleFeatures/deliveryChecklist/examples），语气对齐现有 12 篇。
   - `public/archetypes/{merge-unit,turn-duel}/{hero,interaction,rule,advanced}.webp`：复用 `cover-gen` 蚀刻报纸风管线（`renderCoverWebp('archetype-<key>', <key>, 'card', variant)`），merge-unit/turn-duel 场景在 scenes.ts 早已存在。
   - Demo 无需注册：`public/demos/atomic/{merge-unit,turn-duel}/index.html` 已存在，embed viewer 的静态 demo 分支优先命中（该分支本就为这两个 key 预留）。

### 推断行为变化（收编映射的必然结果，已逐篇核对）

11/31 篇 play 的母型推断变化：
- 7 篇 feature 级 tag 不再推断母型（合成/放置/数值/点击/Roguelike）：clicker、drag-idle、gacha、infinite-upgrades、merge×2、meta-progression → 无回链（taxonomy 保守口径，正确）。
- 2 篇解谜+物理 → 按 tag 书写顺序命中 puzzle（原 physics）。
- 2 篇塔防 → resolve 为 placement（原 choice-strategy；映射注明塔防是组合玩法）。
- 「回合博弈」tag 未在映射表中（映射只有「回合」「棋盘」），tic-tac-toe-showdown 暂无回链 → 已记 BACKLOG tag-map-001，需 ovo_system 侧补条目。

### 验证

- `pnpm typecheck` ✅；`pnpm build` ✅；`pnpm test:smoke` 68/69（/demo/blocks 404 为存量，HEAD 生产实例同样 404）；`pnpm lint` 16 errors 全为存量（本改动文件零问题，已记 BACKLOG lint-debt-001）。
- launch API 重启 :19600 后：`/archetypes/merge-unit`、`/archetypes/turn-duel` 200；sitemap.xml 含 14 母型；match-3 文章 HTML 含 `/archetypes/match-clear` ×2 与「玩法行为」区块；母型页嵌入 demo iframe 200。
- Playwright 截图：ovo_system `tmp/gameslog-archetype-merge-unit.png`、`gameslog-archetype-turn-duel.png`、`gameslog-play-backlink.png`。

### 发布

- main：`f3bae4d`（[archetypes] 补齐 merge-unit/turn-duel 母型页并落地 cluster→pillar 回链）
- `pnpm release` → deploy/gameslog.top：`cf637fc`（产物：main f3bae4d），本地实例已自动重启
- data/plays-views.json 的运行时脏数据（视图计数 fallback 文件）用 `git restore` 还原后再发布，Redis 才是统计主存储
- push 与两端哈希核实见 STATUS「提交记录」

---

## 追加：同步 site-tags v1.0.1（回合博弈 → turn-duel）

ovo_system 侧 `taxonomy/mappings/site-tags.v1.json` 已 bump 至 v1.0.1（30 条目，新增「回合博弈」→ turn-duel），本站跟随同步，关闭 BACKLOG tag-map-001 的站点侧部分。

### 改动

- `src/lib/archetypes/tag-map.ts`：文件头版本注释 v1.0.0 → v1.0.1；「棋盘」行后新增 `回合博弈: { archetype: "turn-duel" }`。

### 验证

- `pnpm typecheck` ✅；`pnpm build` ✅（Compiled successfully，102 静态页）。
- launch API 重启 :19600（launch-stop → launch，进程 ovo-site-becf127c）后 curl 三项：
  - `/play/tic-tac-toe-showdown` HTML 出现 `/archetypes/turn-duel` ✅（BACKLOG 所述缺口已补）
  - `/play/match-3-retention-and-pacing` 仍回链 `/archetypes/match-clear` ✅（无回归）
  - `/play/merge-level-core-loop` 无任何 `/archetypes/*` 链接 ✅（「合成」维持 feature 层，为有意决策，主循环规则仍待协议方决策）

### 发布

- main：`a9a5fa7`（[archetypes] 同步 site-tags v1.0.1：新增「回合博弈」tag → turn-duel 映射）
- `pnpm release` → deploy/gameslog.top：`1526247`（产物：main a9a5fa7）；本轮 `data/plays-views.json` 不脏，无需 git restore
- 两端哈希核实（GIT_SSH_COMMAND 走 ssh.github.com:443）：main 两端 `a9a5fa75a6b47f50521b1bc43c2d697c191a3013`，deploy 两端 `1526247f35613bbfd7329b99165aee994b63adda`

---

## 追加：ContentPack v1.1 站点配合（显式母型回链 + plays-en 目录）

ovo_system 内容生产线升级 v1.1（多语言 + 显式母型落 meta.json），本站侧配合改动。

### 改动

- `src/lib/content/plays.ts`：`PlayMeta` 增加 `archetype` / `archetypeSecondary` / `lang` 可选字段（由生产线 gameslog-mdx 适配器 v1.1 写入 meta.json）。
- `src/app/(site)/play/[slug]/page.tsx`：母型回链**优先读 `meta.archetype`**（经 `isPlayArchetypeKey` 校验），缺失时退回 `inferArchetypeFromTags`（tag-map.ts 保留为兜底）。
- `content/plays-en/README.md`：英文区内容目录（生产线 `gameslog-en` 渠道物化目标）；当前前端只渲染 `content/plays/`，本目录为英文区内容源预留。

### 验证（dev server :3101 真实断言，生产实例 release 后复核）

- 显式优先级：测试文章 meta.archetype 置 puzzle（tags 仍为「消除」）→ 页面回链 `/archetypes/puzzle`（显式覆盖 tag 推断）✅
- 兜底：删除 archetype 字段 → 回链恢复 `/archetypes/match-clear` ✅
- 旧文章无回归：tic-tac-toe-showdown（无 archetype 字段）→ `/archetypes/turn-duel` ✅；match-3-retention-and-pacing → `/archetypes/match-clear` ✅（release 后生产实例 :19600 复核一致）
- `pnpm typecheck` ✅；`pnpm build` ✅

### 发布

- main：`2439e4b`（[content] ContentPack v1.1 站点配合…）；`pnpm release` → deploy/gameslog.top：`1bd787c`（产物：main 2439e4b），本地实例已自动重启
- `data/plays-views.json` 运行时脏数据已 git restore 后发布
- 两端哈希核实（ssh.github.com:443）：main = `2439e4b8adf4bb0f31e69368e8e67726ff4eda2d`，deploy = `1bd787cc9ce86d4d79d7ebf80e5b91ae4eb8f49f`
